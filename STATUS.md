# Status

## Current Scientific State

- Append-only trace semantics and exact 2D hard-max retrieval remain validated
  on the current scope.
- Staged-neural execution stays caveated by legality structure; the widened
  staged branch remains diagnostic-only rather than claim-bearing evidence.
- The precision story is a bounded positive, not a broad robustness claim;
  float32 single-head fails on `12/25` tracked real/organic streams,
  `7/25` already at `1x`, while at least one decomposition stays exact on
  `25/25` tracked streams in the validated suite.
- The current compiled endpoint remains tiny typed bytecode `D0`, with
  deterministic verifier coverage, exact-trace / exact-final-state agreement on
  the frozen starter suite, memory-surface diagnostics, one stress/reference
  follow-up, one harder-suite `R3` exactness gate, and one bounded `R4`
  mechanistic-closure bundle.
- `R6` keeps the completed long-horizon packet exact on current families only:
  all `24` fixed-multiplier rows remain admitted, `8/8` longer-row
  decode-parity checks match exactly, and the narrow multiplier-`8` precision
  companion marks `4/8` boundary-bearing streams with the weaker control
  failing on `2/4` of them.
- `R7` preserved the full `8`-family exact-admitted `R6` surface but profiled
  only the top `4` heaviest family representatives and returned
  `stop_decode_gain_not_material`: all `4/4` profiled rows stayed exact,
  accelerated Hull decode is only about `0.973x` of linear on median, and it
  still runs about `1980.3x` slower per step than the lowered path.
- `R8` has now closed the bounded same-endpoint retrieval-pressure gate:
  all `4/4` harder-family exact rows stayed admitted, the bounded top-`2`
  decode-parity probe matched on `2/2` rows, pressure rose to about `1.249x`
  max event growth and `1.560x` max total candidate-depth growth versus the
  admitted `R6` `8x` source rows, and `E1c` was not triggered.
- `R9` has now closed the bounded real-trace precision companion on the
  admitted `R8` memory streams: all `4/4` screened streams remain
  `effective_here`, one `single_head` `tie_collapse` appears at `1x` on the
  helper-checkpoint-braid-long stream but is covered by the default
  decomposition grid, and the weaker negative control does not fail.
- `R10` has now closed the bounded same-endpoint cost-attribution companion:
  representative admitted-row profiling shows median exact-versus-lowered
  runtime still around `2429.1x`, median retrieval share around `99.8%`,
  harness share effectively negligible, and retrieval dominating on `4/4`
  profiled rows.
- `R11` has now closed the bounded geometry fast-path re-audit: the current
  parity slice stays exact on `5/5` audited cases, preserved standalone
  cache-versus-bruteforce speedup ranges from about `42.8x` to `249.2x`, and
  same-endpoint executor speedup wording remains explicitly blocked.
- `R12` has now closed the bounded append-only executor long-horizon re-audit:
  current exported executor modes remain exact on the preserved `M4` suites,
  the longest heldout countdown still reaches `104` steps, the preserved
  harder `R3` baseline remains contradiction-free, and the next staged harder
  slice is explicit across `24` `R6` rows plus `4` `R8` rows.
- Current `H15` refreeze state is `direct_refreeze_complete`: `R13` remained
  inactive on the exported evidence state, and `R14` remained unjustified on
  the fixed `D0` endpoint.
- The systems result remains mixed; geometry shows a strong asymptotic
  cache-vs-bruteforce win, but the lowered `exec_trace` path is still about
  `1.82x` slower than the best current reference/oracle path on positive `D0`
  suites.
- `H4/E1a/E1b/H5` remains the completed bounded return packet.
- `H6/R3/R4/(inactive R5)/H7` remains the completed bounded exactness /
  mechanism baseline underneath the current same-endpoint packets.
- `H8/R6/R7/H9` remains the completed direct same-endpoint baseline on the
  current `D0` endpoint.
- `H10/H11/R8/R9/R10/H12` remains the latest completed same-endpoint
  follow-up packet on the current `D0` endpoint.
- `H13_post_h12_rollover_and_next_stage_staging` is no longer the active
  stage. `V1_full_suite_validation_runtime_audit` remains the standing bounded operational reference
  under the preserved `H13/V1` handoff.
- `V1` still records `pytest --collect-only -q` success on the current suite,
  and the bounded top-`6` per-file timing follow-up classifies the full-suite
  gate as `healthy_but_slow`.
- The current active post-`P9` operational stage is
  `H15_refreeze_and_decision_sync`.
- `H14_core_first_reopen_and_scope_lock` is now the completed reopened packet
  rather than the active stage.
- The completed reopen wave ran through
  `H14 -> R11 -> R12 -> conditional R13 -> bounded R14 -> H15`.
- `H15` leaves the repo refrozen on the same endpoint, records `R13` as
  inactive plus `R14` as unjustified on current evidence, and requires a new
  explicit plan before any further active science lane starts.
- `pytest --collect-only -q` now returns successfully on the current suite,
  and the bounded timing follow-up completes on `6/6` selected heavy files, so
  the remaining issue is operational runtime budgeting rather than discovery.
- `E1c` remains conditional only and contradiction-only on the frozen `D0`
  boundary.
- Frontend widening is not authorized, README-level release remains acceptable,
  and broader blog/demo prose remains blocked.

## Current Paper State

- The paper bundle has a locked manuscript bundle, appendix package, caption
  notes, layout decisions, and machine-audited public-surface / callout guards
  under `docs/publication_record/`.
- `results/P1_paper_readiness/summary.json` reports the frozen current bundle
  as ready on the current scope.
- `P8` stage is complete on the current frozen scope.
- `P9` stage is complete on the same scope.
- The current active post-`P9` operational stage is
  `H15_refreeze_and_decision_sync` under
  `docs/publication_record/current_stage_driver.md`, with the completed
  `H14/R11/R12` reopen packet preserved and `H13/V1` preserved as the
  governance/runtime handoff.
- `H8/R6/R7/H9` remains the completed direct same-endpoint baseline
  underneath the current stage.
- `H6/R3/R4/(inactive R5)/H7` remains the deeper completed baseline
  underneath the current stage.
- `H10/H11/R8/R9/R10/H12` remains the latest completed same-endpoint
  follow-up packet, and `V1` remains the preserved operational audit
  classifying the full-suite validation-runtime gate as healthy but slow on
  the current suite.
- `E1c` remains conditional only and stays inactive unless the completed
  same-endpoint packets or a later explicit review expose a concrete frozen-`D0`
  contradiction.

## Immediate Next Actions

1. Keep `README.md`, `STATUS.md`, `docs/publication_record/README.md`,
   `docs/publication_record/current_stage_driver.md`, and short public-surface
   syncs aligned on the rule that `H15` is the current refrozen stage,
   `H14/R11/R12` is the completed reopen packet, `H13/V1` is the preserved
   governance/runtime handoff, and no new evidence wave is active yet.
2. Keep `results/H14_core_first_reopen_guard/summary.json`,
   `results/P1_paper_readiness/summary.json`,
   `results/H13_post_h12_governance_stage_health/summary.json`,
   `results/release_worktree_hygiene_snapshot/summary.json`,
   `results/P5_public_surface_sync/summary.json`,
   `results/P5_callout_alignment/summary.json`,
   `results/release_preflight_checklist_audit/summary.json`,
   `results/H2_bundle_lock_audit/summary.json`,
   `results/P10_submission_archive_ready/summary.json`, and
   `results/V1_full_suite_validation_runtime_audit/summary.json` green while
   the current refrozen stage remains active.
3. Keep `results/H10_r7_reconciliation_guard/summary.json`,
   `results/H11_post_h9_mainline_rollover_guard/summary.json`,
   `results/H8_driver_replacement_guard/summary.json`, and
   `results/H6_mainline_rollover_guard/summary.json` synchronized with the
   claim/public-surface ledgers under the current `H15` refrozen state.
4. Treat `results/V1_full_suite_validation_runtime_timing_followup/summary.json`
   as the current validation-hygiene reference: `pytest --collect-only -q`
   plus bounded top-`6` per-file timings classify full `pytest -q` as a
   healthy but multi-minute gate, so reserve it for long unattended windows
   rather than short waits.
5. Treat `results/H13_post_h12_governance_stage_health/summary.json` as the
   preserved `H13/V1` handoff reference; it should stay green alongside the
   `V1` timing classification, preserved baseline guards, outward-sync audit,
   and archive-handoff audit while `H15` keeps the repo refrozen.
6. Treat `results/release_worktree_hygiene_snapshot/summary.json` as the
   current repo-cleanliness reference for outward sync commits; a dirty tree
   blocks release-facing commits but does not by itself reopen scientific
   scope.
7. Use `results/H15_refreeze_and_decision_sync/summary.json` as the current
   decision reference: keep `R13_small_model_executor_reactivation` inactive,
   keep `R14_bounded_compiled_probe` unjustified unless a new bounded gap
   appears, and require a new explicit plan before reopening the mainline.
8. Treat `results/release_preflight_checklist_audit/summary.json` as the
   current outward-sync control reference alongside the worktree snapshot.
9. Keep derivative-only writing downstream of the evidence state, carry the
   completed `H14/R11/R12/H15` reopen/refreeze packet plus the earlier
   `H10/H11/R8/R9/R10/H12` outputs in the refrozen checkpoint, and do not
   widen the stage beyond the completed `H14` scope lock.

## Known Blockers

- Broader compiled demos remain blocked by the current no-widening boundary.
- Broader outward narrative remains blocked by the restrained-release policy.
- Current-scope end-to-end systems superiority remains unsupported by the
  current systems gate.
- No active lane authorizes arbitrary compiled-language claims or broader
  runtime generalization.
