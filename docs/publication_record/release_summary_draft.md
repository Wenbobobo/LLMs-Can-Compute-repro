# Release Summary Draft

## Narrow Target

This repository supports a narrow execution-substrate claim rather than a
broad “LLMs are computers” thesis. On the current validated scope, the project
supports three linked statements: deterministic computation can be encoded as
an append-only execution trace; exact latest-write retrieval over that trace
can be implemented with structured 2D hard-max retrieval; and those primitives
are sufficient for a small exact executor on the admitted bounded bundle.

## Current Endpoint

The strongest justified useful-case executor-value lane is closed negative.
`R62` preserved exactness on the admitted native useful-kernel rows, but it
did not preserve bounded value relative to the direct native comparators.
`H58` therefore remains the binding value-negative closeout, and `H59`
remains the preserved reproduction-gap packet above it.

The current live control state is now:

- `H66_post_p90_archive_replace_terminal_stop_packet`
- `P91_post_h66_next_planmode_handoff_sync`
- `P90_post_p89_archive_replace_screen_and_replacement_decision`
- `P87_post_p86_paper_spine_refresh_and_salvage_shortlist`

`H66` makes terminal stop the current docs-only packet, `P91` freezes the next
planning prompts on that stop posture, `P90` closes the remaining archive/
replace screen with no further salvage, and `P87` remains the live paper-spine
refresh layer on the clean branch.

## Paper-Facing Partial Falsification

The correct outward shorthand remains:

- narrow mechanistic reproduction survives;
- broad headline reproduction did not land;
- the strongest justified executor-value lane is closed negative; and
- any future continuation still requires a later explicit packet and a
  materially different cost structure.

`H43` remains a preserved paper-grade useful-case endpoint. The later closeout
stack does not widen that endpoint into arbitrary `C`, broad Wasm, runtime
reopen, or same-endpoint practical systems superiority.

## Future-Route Boundary

The only future family still preserved on paper remains the dormant
coprocessor-style route recorded under `F38/R63`. It remains a dormant
non-runtime dossier, not an active lane.

The unresolved gating fields remain:

- retrieval-head cost share;
- query:insert ratio;
- tie frequency and tie-handling burden; and
- a materially different cost model relative to `R62/H58`.

That route remains dormant. The current downstream recommendation is explicit
stop, archive polish, or no further action, not further scientific widening.
