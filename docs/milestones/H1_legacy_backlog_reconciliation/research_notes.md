# Research Notes

- A frozen negative-control branch is still useful scientific output; its
  backlog should not masquerade as unfinished positive evidence.
- Scope freezes should be visible in the repository structure, not just in
  prose summaries.
- Legacy unchecked rows are operational debt in an unattended workflow because
  they can redirect effort away from the actual active phase.
