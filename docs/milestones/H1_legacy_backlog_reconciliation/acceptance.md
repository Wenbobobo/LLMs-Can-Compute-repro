# Acceptance

- every currently unchecked legacy row has exactly one disposition in the
  classification matrix;
- affected historical `todo.md` files no longer present unchecked rows as
  silently active work;
- future unattended agents can distinguish active, frozen, superseded, and
  conditional backlog without reconstructing prior project phases.
