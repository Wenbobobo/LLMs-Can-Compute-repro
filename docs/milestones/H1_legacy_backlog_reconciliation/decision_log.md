# Decision Log

## 2026-03-19

- Reconcile legacy backlog centrally instead of editing old milestone history
  into a false appearance of completion.
- Use only four dispositions: `active_now`, `frozen_negative_control`,
  `superseded_by_scope_freeze`, and `conditional_future_reopen`.
- Treat current `P6`/`P7` work as the active lane; do not classify those rows
  inside `H1`.
