# Acceptance

- the lane exports a bounded boundary map;
- first-fail rows are minimized and reproducible;
- negative and positive rows are both preserved;
- no new repair lane is silently opened inside `R21`.
