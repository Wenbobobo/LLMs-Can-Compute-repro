# Blocked Hypotheses

- `R21` should not widen the `D0` boundary while mapping failures.
- A failure-rich grid is still a useful scientific result if it localizes the
  boundary cleanly.
- `R21` does not justify a new repair lane unless `H19` says so explicitly.
