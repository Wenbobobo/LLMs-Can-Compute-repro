# Main Delta Summary

Current comparison target:

- `main`

Current clean audit branch:

- `wip/f16-h38-p26-exec`

Preserved prior clean source branch:

- `wip/p25-f15-h37-exec`

Current reading:

- `main` is still materially behind the active Origin-core line;
- the ahead-of-`main` delta includes both the already-landed Origin-core
  scientific/runtime packet and the newer docs/control waves;
- the current branch should be reviewed and promoted in packets rather than by
  one broad merge while `main` is dirty.

Promotion default:

- `audit_only`
