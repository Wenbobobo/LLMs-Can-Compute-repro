# TODO

- [x] Fix the first frontend boundary at the spec level.
- [x] Decide that the first comparison is exact differential testing.
- [x] Freeze the tiny-bytecode verifier / lowering / harness contract.
- [x] Implement the typed-bytecode harness before any compiled-demo work.
- [x] Add the first memory-surface follow-up without widening beyond the same
  `D0` claim row.
- [x] Create dedicated planning milestones for `M6_boundary_freeze` and
  `M6_stress_reference_followup`.
- [x] Freeze the current control-flow + memory-surface slice as the last
  pre-demo widening unless a concrete contradiction appears.
- [x] Define one serious end-to-end stress family that stays inside the same
  tiny typed-bytecode surface.
- [x] Compare the current positive/negative suite against an external reference
  interpreter.
- [x] Reassess whether any broader compiled demo is justified only after
  `P3_paper_freeze_and_evidence_mapping`,
  `R2_systems_baseline_gate`, and
  `M7_frontend_candidate_decision` close positively; current answer is no on the frozen scope.
