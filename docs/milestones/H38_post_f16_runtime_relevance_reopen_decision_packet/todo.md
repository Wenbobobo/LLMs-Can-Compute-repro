# H38 Todo

- [x] Keep the two-outcome fork explicit:
      `keep_h36_freeze` versus
      `authorize_r41_origin_runtime_relevance_threat_stress_audit`.
- [x] Keep `H36` explicit as the active routing/refreeze packet underneath the
      current stack.
- [x] Keep `H37` explicit as the preserved prior docs-only decision packet.
- [x] Make the default decision basis explicit:
      `F16` produced zero `execution_ready` candidates on the fixed landed
      `R40` row pair.
- [x] Keep `R41`, `R29`, `F3`, and `F2` non-active by default.
