# Status

Provisioned on 2026-03-20 under active `H14`.

- near-term target: longer-horizon append-only/latest-write execution on the
  current `D0` endpoint;
- reference code lives in `src/exec_trace/` and `src/model/free_running_executor.py`;
- current historical references are `R3` exact execution stress and `R4`
  mechanistic retrieval closure.
- current execution interface is now captured in `runbook.md` and
  `artifact_contract.md`;
- current reopened artifact lands in
  `results/R12_append_only_executor_long_horizon/`.
