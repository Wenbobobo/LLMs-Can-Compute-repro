# Acceptance

- longer-horizon append-only execution is checked on the same endpoint;
- free-running exactness is reported explicitly where it holds and where it
  fails;
- failure modes are localized rather than left generic.
