# F17 Post-H38 Same-Substrate Exit Criteria Bundle

Planning-only route-selection bundle after the completed `H38` keep-freeze
decision.

`F17` exists to make later same-substrate continuation criteria explicit
without authorizing a new lane now.

This bundle preserves:

- `H38` as the current docs-only decision packet;
- `H36` as the underlying active routing/refreeze packet;
- `R41` as deferred;
- `F9` and `F11` as preserved broader future families rather than active
  follow-ons.
