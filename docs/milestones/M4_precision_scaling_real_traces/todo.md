# Todo

- Sweep longer horizons and multiple decomposition bases on the current offset
  families.
- Add larger mixed traces so the real-trace suite is not dominated by one loop
  family.
- Separate tie-collapse failures from strict wrong-address inversions in the
  summary tables.
