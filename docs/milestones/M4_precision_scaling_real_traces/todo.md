# Todo

- [x] Sweep longer horizons and multiple decomposition bases on the current
  offset families.
- [x] Add larger mixed traces so the real-trace suite is not dominated by one
  loop family.
- Separate tie-collapse failures from strict wrong-address inversions in the
  summary tables.
- Add organically longer traces instead of only offset-transformed current
  programs.
