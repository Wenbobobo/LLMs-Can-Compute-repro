# Brainstorm Log

- Address-offset programs were the fastest way to stress magnitude without
  changing semantics.
- Horizon inflation was chosen before building much longer programs because it
  isolates the epsilon-resolution effect directly.
- Alternating-memory offset streams were added so the sweep would not be driven
  mostly by one loop family.
