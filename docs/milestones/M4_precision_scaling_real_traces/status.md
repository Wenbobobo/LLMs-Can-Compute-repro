# Status

- Added real-trace precision checking over extracted latest-write streams.
- Added address-offset loop, ping-pong, and alternating-memory families for
  stress tests.
- Added `results/M4_precision_scaling_real_traces/horizon_base_sweep.json`.
- Current milestone state: materially stronger but still narrow; the offset
  suite now covers multiple families and explicit horizon/base sweeps, but it
  is still not a full long-horizon validation.
