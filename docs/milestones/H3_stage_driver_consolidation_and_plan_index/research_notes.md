# Research Notes

- The main risk is documentation drift rather than scientific drift.
- The current repo state already has a locked checkpoint; the problem is that
  several governance docs still look like stage drivers when read in isolation.
- This lane should reduce future unattended planning ambiguity, not add new
  paper scope.
