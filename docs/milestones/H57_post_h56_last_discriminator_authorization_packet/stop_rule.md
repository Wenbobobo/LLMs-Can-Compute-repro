Stop immediately at `H58` if `R62`:

- breaks exactness on any declared row
- fails to make accelerated faster than linear on the longest row of each
  kernel
- remains outside one order of magnitude of the external scalar comparator on
  every kernel
