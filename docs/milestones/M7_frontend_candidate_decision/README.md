# M7 Frontend Candidate Decision

Goal: decide whether any frontend widening is scientifically justified after
`P3`, `R1`, and `R2`.

This is a decision milestone, not a build milestone.

Allowed outcomes:

- keep the current tiny typed-bytecode boundary as the endpoint;
- approve one minimal next frontend candidate with explicit scope;
- record a no-go and keep widening blocked.
