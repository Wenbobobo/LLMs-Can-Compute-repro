# Result Digest

`R9` exported the bounded real-trace precision companion on exact-admitted
`R8` rows and kept the precision story companion-only on the same fixed `D0`
endpoint.

## What `R9` closed

- inherited its row set strictly from the `4` exact-admitted `R8` rows and
  kept the precision follow-up on memory streams only;
- screened `4/4` admitted memory streams under the bounded `1x/2x` horizon
  grid and placed all `4/4` in the `effective_here` bucket;
- recorded one bounded `single_head` `tie_collapse` at `1x` on the
  `helper_checkpoint_braid_long` memory stream, but the default decomposition
  grid remained fully passing and the weaker base-256 negative control did not
  fail;
- kept `E1c` inactive and handed the packet forward to `R10`.

## What `R9` did not do

- did not reopen a stack-wide sweep or make a broader unseen-family precision
  claim;
- did not widen the compiled endpoint or change the `D0` claim boundary;
- did not convert one bounded companion screen into a general robustness
  guarantee.
