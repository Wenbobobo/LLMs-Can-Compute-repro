# Status

Opened on 2026-03-20.
Closed on 2026-03-20 with `bounded_real_trace_precision_boundary_companion`.

- `R9` stayed companion-only on exact-admitted `R8` same-endpoint rows;
- the screened row set closed as `4/4` admitted memory streams, all classified
  `effective_here`;
- one stream showed a bounded `single_head` `tie_collapse` at `1x`, but the
  default decomposition grid still stayed fully passing and the weaker base-256
  negative control did not fail;
- `E1c` stayed inactive and the next live lane is `R10`.
