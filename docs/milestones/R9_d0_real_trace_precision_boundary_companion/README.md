# R9 D0 Real-Trace Precision Boundary Companion

Keep the precision story companion-only on current or newly admitted `D0`
rows and sharpen the real-trace boundary without reopening an open-ended sweep.

The bounded companion is now closed on the current packet: it screens the
exact-admitted `R8` memory streams only, keeps all `4/4` screened streams in
the `effective_here` bucket, and hands the packet forward to `R10`.
