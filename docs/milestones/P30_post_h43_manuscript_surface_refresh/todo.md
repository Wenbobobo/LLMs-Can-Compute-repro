# P30 Todo

- refresh `paper_outline.md` so it treats `H43` as the current manuscript
  endpoint and `H32/H34` as preserved earlier compiled-boundary context;
- refresh the contradictory current-state block in `STATUS.md` so it stops
  presenting `H36/P24/H34` as the current paper-facing operational surface;
- refresh `manuscript_bundle_draft.md`, `paper_bundle_status.md`, and
  `release_summary_draft.md` so the manuscript-facing prose baseline no longer
  terminates on landed `H34`;
- refresh `derivative_material_pack.md`,
  `abstract_contribution_pack.md`, and
  `external_release_note_skeleton.md` so derivative helper docs stop implying
  that the preserved first `D0` boundary is the whole current paper endpoint;
- record the wave in plan, milestone, publication-record, manifest, and
  active-wave handoff surfaces; and
- export a focused `P30` summary/checklist/snapshot packet for the refreshed
  manuscript surface.
