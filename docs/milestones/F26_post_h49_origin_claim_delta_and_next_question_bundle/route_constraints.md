# F26 Route Constraints

- `R51` is the only next runtime candidate fixed here.
- `R52` is the only later comparator/value gate fixed here.
- `H50` is the only follow-up packet fixed here.
- `F27` is saved as planning-only and cannot authorize execution from `F26`.
- broader useful-case growth cannot reopen by momentum from positive `R50`.
- trainable-executor growth cannot reopen by momentum from preserved `R45`.
- transformed-model or "programs into weights" work remains blocked pending a
  positive `H50`.
- dirty root `main` remains out of scope for scientific execution in this wave.
