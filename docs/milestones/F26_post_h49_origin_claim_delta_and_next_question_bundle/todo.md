# F26 Todo

- save the dedicated post-`H49` next-wave design;
- define one claim-delta matrix that maps claim layers `A/B/C/D` onto landed
  evidence and unresolved gaps;
- define one next-question note that chooses substrate-first falsification
  instead of useful-case widening by momentum;
- define one route-constraints note that fixes `R51 -> R52 -> H50` ordering;
- keep `R51` as the only next runtime candidate and `R52` as the only later
  comparator/value gate; and
- keep broader Wasm, arbitrary `C`, trainable-executor growth, transformed
  model entry, and demo-first widening out of scope unless a later explicit
  packet changes the boundary.
