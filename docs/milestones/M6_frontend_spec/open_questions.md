# Open Questions

These questions are downstream of the now-fixed boundary.

- After the tiny bytecode milestone, should the next frontend be Wasm-like or a
  richer in-repo bytecode extension?
- If a second typed pass is needed, should it add calls/returns first or local
  slots first?
- Which later long-horizon bytecode family best pressures the substrate without
  turning into a presentation-first demo?
