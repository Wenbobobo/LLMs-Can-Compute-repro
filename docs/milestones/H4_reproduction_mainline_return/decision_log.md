# Decision Log

- Keep the locked submission/release checkpoint as baseline rather than opening
  a new broad research phase.
- Express the active scientific return as `H4` plus serial `E1a` -> `E1b` ->
  optional `E1c` -> `H5`.
- Treat paper/blog work as downstream bookkeeping during this stage.
