# Research Notes

- `P8` is not a prose-beautification phase. It is a lock phase.
- Any mismatch discovered during `P8` should be treated as a bundle conflict
  first, and only escalated into `E1` if it truly requires new evidence.
- The scientific endpoint remains the narrow execution-substrate story already
  exported in `P3`, `R1`, `R2`, and `M7`.
