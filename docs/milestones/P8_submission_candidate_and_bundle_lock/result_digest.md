# Result Digest

`P8` closed the submission-candidate bundle-lock pass on the frozen paper
scope. The manuscript bundle, appendix boundary, claim ladder, claim/evidence
table, negative-results ledger, and threats-to-validity ledger now read as one
consistent frozen package. The explicit blocked claims remain blocked:
general LLM computation, arbitrary C reproduction, broader compiled demos,
fair-regime staged-pointer exactness, broad long-horizon precision robustness,
and current-scope end-to-end runtime superiority.

The milestone did not open a new evidence wave. Instead, it tightened the
paper-facing bundle to the frozen endpoint already supported by the current
ledger set:
append-only traces, exact latest-write retrieval, bounded precision on the
validated suites, and the tiny typed-bytecode `D0` compiled boundary.

The locked bundle is therefore submission-candidate ready on the current
scope. Any reopening now requires an explicit new scope decision and a new
evidence pass.
