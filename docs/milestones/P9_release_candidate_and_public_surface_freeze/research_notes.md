# Research Notes

- `P9` should compress the frozen endpoint, not reinterpret it.
- If outward wording reveals a missing evidence anchor, route the issue through
  `E1` instead of softening the language.
