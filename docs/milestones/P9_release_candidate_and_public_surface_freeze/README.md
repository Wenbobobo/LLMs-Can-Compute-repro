# P9 Release Candidate and Public Surface Freeze

Goal: freeze the restrained public surface around the locked paper-facing
bundle once `P8` and `H2` are satisfied.

Scope:

- synchronize README, STATUS, and the short release summary;
- keep the blocked-blog state explicit and derivative-only;
- prepare one clean outward release-candidate handoff.

Non-goals:

- no blog release by default;
- no claim expansion;
- no new scientific experiments.
