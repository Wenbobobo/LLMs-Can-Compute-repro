# H42 Todo

- compare `authorize_r44_origin_restricted_wasm_useful_case_execution_gate`
  against `hold_at_r43_and_continue_bounded_consolidation` and
  `keep_h41_r43_r45_state_and_continue_planning_only`;
- keep `H41`, `H36`, completed exact `R43`, completed coequal `R45`, and
  `F20` explicit inside the packet;
- keep `R41`, `F11`, `R29`, `F3`, arbitrary `C`, and general “LLMs are
  computers” rhetoric non-active here; and
- keep merge explicit through `P27` rather than treating branch hygiene as a
  scientific authorization.
