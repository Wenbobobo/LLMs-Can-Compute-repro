# TODO

Legacy note: the remaining unchecked rows are tracked in
`docs/milestones/H1_legacy_backlog_reconciliation/classification_matrix.md`
and are not active on the current frozen paper scope by default.

- [x] Add a third event-grouped serialization
- [x] Compare vocab, length, teacher forcing, and first-error position
- Maintenance-only note: architecture-side intervention is dormant unless
  `R4` explicitly needs one final baseline repair attempt on the active suite.
- Maintenance-only note: the branch should otherwise be treated as a frozen
  negative control and kept only for reproducible reruns.
