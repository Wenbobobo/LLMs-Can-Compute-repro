# Acceptance

- one clean-worktree closeout path is explicit before any later explicit
  packet;
- the `H30` packet and docs sweep can be copied as one or two reviewable
  path-scoped packets;
- the clean worktree path/branch are fixed and verifiable;
- no broader runtime execution or scope widening is implied.
