# Todo

- [x] Save the post-`H30` explicit-next-wave design before execution.
- [x] Fix one clean-worktree runbook before any later explicit packet.
- [x] Create a clean `h30-clean` scaffold on `wip/p17-h30-clean`.
- [x] Define the narrowest reviewable packet split for the
  `H28/R34/R35/H29/R36/R37/H30` bundle and the current docs-only closeout
  sweep.
- [x] Copy the approved review subset into the clean worktree and inspect
  `git status --short`.
- [x] Re-run focused checks in the clean worktree before any staging or commit.
- [x] Stage and commit the prepared clean-worktree subset.
- [ ] Keep `R29`, `F3`, and any later compiler-boundary extension blocked until
  a later explicit packet is deliberately opened.
