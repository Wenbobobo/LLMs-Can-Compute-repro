# R44 Required Outputs

- one lowering manifest;
- one kernel suite report with exactness and cost tables;
- one summary with explicit verdict labels:
  `keep_semantic_boundary_route`,
  `mixed_nonunique`,
  `useful_case_surface_break`,
  `useful_case_surface_supported_narrowly`;
- one stop-rule record.
