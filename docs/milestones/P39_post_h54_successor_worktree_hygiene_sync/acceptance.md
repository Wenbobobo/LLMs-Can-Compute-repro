# Acceptance

`P39` is acceptable only if it codifies:

- clean-worktree-only execution;
- no-merge posture for dirty root `main`;
- artifact slimming for raw traces and files above roughly `10 MiB`; and
- split commit cadence for planning, runtime, comparator, and decision packets.
