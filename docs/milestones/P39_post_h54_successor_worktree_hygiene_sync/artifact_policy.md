# Artifact Policy

- keep summaries and manifests in git;
- keep raw row dumps and long per-step traces out of git by default;
- treat files above roughly `10 MiB` as out-of-git unless review-critical.
