# Result Digest

- Pending release: new memory instrumentation artifacts should quantify how many exact-trace rows now include `heap_diff` entries without changing control-flow diagnostics.
- The ledger should explicitly state whether any `C3e` precision boundaries move when heap/stack tagging is applied.
