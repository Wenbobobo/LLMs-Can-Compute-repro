# Acceptance

- memory instrumentation reports include deterministic heap/stack maps for every exact-trace row;
- verifier enforces memory typing guards without altering control-flow decisions already recorded in `M6_typed_bytecode_harness`;
- new differential comparisons highlight when memory instrumentation changes exact-final-state outcomes;
- `results/M6_memory_surface_followup` exports contain JSON/CSV ledgers and the updated claim ladder entry referencing `C2h`/`C3e` precision boundaries;
- README, status, and todo remain aligned with the frozen claim scope and describe only this follow-up work.
