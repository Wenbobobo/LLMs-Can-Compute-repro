# Decision Log

- Keep `M6_typed_bytecode_harness` control-flow artifacts fixed while memory surfaces are added.
- Only produce new claims if the memory instrumentation alters `C2h`/`C3e` outcomes described in a ledger entry.
- Record any discovered aliasing divergences as machine-readable artifacts so future milestones can reason about them.
