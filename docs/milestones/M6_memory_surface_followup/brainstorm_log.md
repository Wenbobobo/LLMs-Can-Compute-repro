# Brainstorm Log

- Track memory surface differences by adding deterministic `heap_diff`/`stack_diff` records alongside existing exact-trace rows.
- Repurpose the ledger-first discipline to make memory diagnostics first-class, so every unattended export updates `results/M6_memory_surface_followup`.
- If aliasing invariants shift, capture the precise textual explanation so claim updates remain traceable.
