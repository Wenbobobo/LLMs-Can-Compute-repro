# Decision Log

- Keep the completed `H4/E1a/E1b/H5` packet as the immediate baseline rather
  than reopening a fresh broad evidence wave.
- Make `R3` exact-execution stress and `R4` mechanistic closure the default
  serial lanes.
- Keep `R5` conditional and `E1c` contradiction-only.
- Convert historical open checkboxes into dormant notes so unattended runs do
  not mistake them for active obligations.
