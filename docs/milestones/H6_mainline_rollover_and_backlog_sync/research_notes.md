# Research Notes

- The scientific center of gravity is still exact execution on append-only
  traces, not the broader blog headline.
- `R3` and `R4` are valuable because they keep evidence on the same endpoint
  while increasing falsifiability.
- The backlog cleanup is not cosmetic; it prevents future unattended work from
  leaking effort into negative-control rescue or historical side quests.
