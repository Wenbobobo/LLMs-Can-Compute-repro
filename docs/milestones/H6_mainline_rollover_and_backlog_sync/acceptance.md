# Acceptance

- the active stage is explicitly the bounded `H6/R3/R4/(optional R5)/H7`
  packet;
- `README.md`, `STATUS.md`, and publication-record entry docs agree on that
  packet;
- dormant historical milestones no longer present stale active checkboxes;
- the new `H6` guard export reports zero blocked items;
- no current doc widens the claim surface beyond the frozen narrow endpoint.
