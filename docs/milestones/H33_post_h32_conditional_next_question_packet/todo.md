# H33 Todo

- [ ] Save one post-`H32` design before any new runtime work.
- [ ] Compare exactly two outcomes:
  `freeze_compiled_boundary_as_complete_for_now` and
  `authorize_one_origin_core_substrate_question`.
- [ ] Keep `H32` explicit as the current active packet throughout.
- [ ] Name at most one future runtime lane if any runtime lane is authorized.
- [ ] Preserve `R29`, `F3`, and broader compiler/demo claims as blocked by
  default.
