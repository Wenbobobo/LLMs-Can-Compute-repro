# R17 D0 Full-Surface Runtime Bridge

Measure same-endpoint runtime on the full admitted `R8 + R15` surface after
`R16`, then keep deeper attribution bounded to the smallest subset needed to
decide whether `R18` stays inactive.
