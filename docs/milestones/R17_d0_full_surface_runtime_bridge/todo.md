# Todo

- [x] Bind `R17` to the admitted `R8 + R15` surface via the `R16` runtime
  handoff.
- [x] Implement full-surface runtime export and bounded focused attribution.
- [x] Add targeted contract tests for surface loading, focused selection, and
  `R18` trigger logic.
- [x] Finish the real long-running export and record the resulting bridge
  metrics.
- [x] Decide whether the real output names a bounded `R18` target or hands
  directly to `H17`.
- [x] Sync any downstream control-plane wording only after the real export is
  stable.
- [ ] Decide whether `R18` should test staged or pointer-like retrieval only on
  `helper_checkpoint_braid_long`, or whether the named target still needs one
  tighter decomposition before repair work starts.
