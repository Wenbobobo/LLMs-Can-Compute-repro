# Status

Implementation and real export completed on 2026-03-20.

- `R17` consumed the `R16` runtime handoff and profiled all `8` admitted
  `R8 + R15` surface rows;
- no contradiction candidate appeared, so `E1c` remained inactive;
- same-endpoint decode gain stayed immaterial and the lowered-endpoint bridge
  remained far open;
- bounded focused attribution named one concrete `R18` target:
  `helper_checkpoint_braid_long` on `retrieval_total`.
