# R42 Required Outputs

- one execution manifest;
- one task table with fixed task ids and length buckets;
- one measurement table covering exactness, duplicates, ties, and precision;
- one summary with explicit verdict labels:
  `keep_semantic_boundary_route`,
  `mixed_nonunique`,
  `retrieval_contract_break`;
- one stop-rule record.
