# Experiment Matrix

- Inputs:
  new organic latest-write streams,
  reference exact-read baseline,
  current precision schemes.
- Outputs:
  screening rows,
  conditional base sweeps,
  failure-type summaries,
  claim-impact note for `C3e`.
