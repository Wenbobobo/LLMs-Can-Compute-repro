# Brainstorm Log

- Add streams that are not simple address-offset wrappers around existing
  programs.
- Keep high-address memory, branch-heavy replay, and stack-memory interleaving
  all represented.
- Preserve failure-type accounting, not only pass/fail rows.
