# Decision Log

- Keep schemes frozen to `single_head`, `radix2`, and `block_recentered`.
- Keep base `64` as the screening default.
- Run wider base sweeps only when the screening stage shows a boundary signal.
