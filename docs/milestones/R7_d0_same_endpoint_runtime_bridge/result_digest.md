# Result Digest

`R7` exported the bounded same-endpoint runtime bridge on the current `D0`
endpoint and returned a conservative stop.

## What `R7` closed

- preserved the full `8`-family exact-admitted `R6` index and profiled only
  the top `4` heaviest family representatives on the same endpoint in one
  bounded single-pass runtime bridge;
- kept all profiled rows exact, with `0` contradiction candidates and exact
  linear-versus-accelerated trace / final-state agreement throughout;
- measured accelerated Hull decode at only about `0.973x` of linear on median,
  with the accelerated exact path still about `1980.3x` slower per step than
  the lowered endpoint on the profiled rows;
- closed with `stop_decode_gain_not_material`, left `R5` not justified, and
  kept `E1c` inactive.

## What `R7` did not do

- did not reopen the broader systems lane;
- did not authorize frontend widening or arbitrary compiled-language claims;
- did not turn the same-endpoint runtime stop into a contradiction claim.
