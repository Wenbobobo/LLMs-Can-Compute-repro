# R7 D0 Same-Endpoint Runtime Bridge

Measure decode-level runtime gains inside the exact `D0` endpoint using the
full exact-admitted `R6` family set as the bounded input surface on the same
endpoint.
