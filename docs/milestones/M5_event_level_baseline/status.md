# Status

- Implemented and exported the event-level standard softmax baseline.
- The branch currently behaves as a stronger negative control than the older
  flat-token variants because it fails even after the representation change.
- Current milestone state: likely ready to freeze unless a very narrow repair
  presents itself.
