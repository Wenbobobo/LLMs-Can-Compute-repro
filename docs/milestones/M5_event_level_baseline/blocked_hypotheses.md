# Blocked Hypotheses

- Flat-token serialization was not the whole problem.
- Moving to the shared event target did not rescue the standard softmax
  baseline, so representation cleanup alone is not enough for this branch.
