# Research Notes

- The original discussions consistently separate the narrow executor claim from
  blog rhetoric; this milestone is where that distinction becomes durable.
- `C2h` is a closure milestone, not a springboard for re-opening staged-pointer
  optimism.
- `C3e` is stronger as a boundary statement than as a scale-up promise.
- `D0` is strongest when framed as “first compiled frontend boundary” rather
  than “compiled demos are solved.”
