# P3 Paper Freeze and Evidence Mapping

Goal: freeze the paper-grade claim wording around the evidence already present
in the repository.

Scope:

- map `D0`, `C2h`, `C3e`, and key negative controls to exact artifacts;
- decide which outputs are mandatory figures/tables versus appendix-only
  companions;
- keep README/blog language strictly downstream of the paper freeze.

Out of scope:

- new frontend widening;
- new claim layers;
- demo-first communication.
