# Acceptance

- trigger conditions are explicit;
- non-goals are explicit;
- no widened experiment is implemented here;
- a future agent can decide whether a frontier plan is justified without
  rereading the entire repo history.
