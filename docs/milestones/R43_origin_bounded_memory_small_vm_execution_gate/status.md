# R43 Status

- deferred future bounded-memory small-VM execution gate;
- requires `R42` to stay exact first;
- fixes bounded loops, branching, and optional single-layer calls as the next
  execution obligations;
- keeps full-trace and final-state exactness mandatory.
