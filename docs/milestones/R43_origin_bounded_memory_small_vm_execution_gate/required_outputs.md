# R43 Required Outputs

- one execution manifest;
- one program table with fixed program ids and length buckets;
- one exactness table for trace match and final-state match;
- one summary with explicit verdict labels:
  `keep_semantic_boundary_route`,
  `mixed_nonunique`,
  `bounded_memory_vm_break`;
- one stop-rule record.
