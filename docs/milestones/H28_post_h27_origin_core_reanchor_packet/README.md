# H28 Post-H27 Origin-Core Reanchor Packet

Executed reanchor packet after `H27`.

`H28` exists to pivot the active stage away from the closed same-endpoint
recovery narrative and toward the narrower Origin-core reproduction ladder.
The packet preserves `H27` as the negative closeout of the old wave, keeps
`R29/F3` blocked, and opens the next justified order as `R34 -> R35 -> H29`.

The reanchored scientific target is:

- append-only execution traces;
- exact `2D` hard-max retrieval and tie semantics;
- a small exact stack/VM executor before any wider compiler or demo claim.
