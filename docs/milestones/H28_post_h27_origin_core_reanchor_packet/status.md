# H28 Status

- `H27` remains the preserved closeout packet for the old same-endpoint
  recovery wave.
- `H28` is now the active planning and control packet.
- The active target is no longer "rescue the frozen `D0` endpoint by
  momentum"; it is "reproduce the defensible Origin-core A/B/C claims with
  explicit gates."
- `R29` and `F3` remain blocked future lanes.
- `F2` remains planning-only.
