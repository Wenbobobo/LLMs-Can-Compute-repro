# Decision Log

- This branch is a baseline, not a substitute for the geometry claim.
- Start with data representation and model definition before any training claim.
- Keep Torch optional until this branch becomes an active training target.
