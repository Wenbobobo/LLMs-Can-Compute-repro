# Result Digest

`R4` exported a bounded mechanistic-closure bundle on the current positive `D0`
suites.

## What `R4` closed

- bridged all `32` current positive `D0` programs back to latest-write,
  stack, control, and deterministic local-transition primitives;
- exported `4290` source-event observations with exact linear-versus-Hull
  agreement and no parity failures;
- kept staged-pointer and provenance artifacts diagnostic-only as mask-strength
  companions rather than claim-bearing mainline evidence;
- left `E1c` inactive and did not justify `R5` on the current scope.

## What `R4` did not do

- did not widen frontend, language, or systems scope;
- did not reopen softmax-baseline rescue work;
- did not turn the diagnostic staged branch into the main mechanism claim.
