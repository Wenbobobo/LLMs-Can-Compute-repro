# Acceptance

- positive `D0` rows are explainable in the allowed primitive set;
- `linear` and `Hull` retrieval agree at the source-event level;
- diagnostic companions remain separate from the main mechanism claim;
- unexplained dependencies are recorded explicitly.
