# Research Notes

- The most valuable output is a clean mechanism statement, not another
  high-level executor narrative.
- Source-event agreement matters more than only matching final states.
- The current positive `D0` suite is already large enough to stress all four
  allowed primitive classes without reopening wider language scope.
- Diagnostic reuse from staged-pointer and provenance artifacts is only about
  mask strength; it should not be allowed to substitute for the exact `D0`
  mechanism story.
