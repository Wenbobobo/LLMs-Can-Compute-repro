# Decision Log

- Keep `R4` on the same positive `D0` suites rather than reopening older
  wider-scope model branches.
- Treat staged-pointer and provenance work as companion diagnostics, not as the
  mainline claim surface.
- Use source-event bridge rows over lowered reference traces instead of another
  end-to-end executor narrative.
- Treat `R5` as inactive unless `R4` exposes a same-scope systems reason;
  current mechanistic closure did not justify that handoff.
