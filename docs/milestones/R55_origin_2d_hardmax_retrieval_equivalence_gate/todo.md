# R55 Todo

- [x] define one transparent reference latest-relevant-state lookup;
- [x] define one claimed `2D` hard-max retrieval path on the same query
  contract;
- [x] fix one bounded task matrix covering overwrite-after-gap, stack-slot,
  duplicate-max, coordinate-offset, and tie cases;
- [x] export exact value parity and maximizer-row parity on every read;
- [x] localize the first failure explicitly if equivalence breaks; and
- [x] stop the mechanism lane immediately if exact equivalence fails or hand
  the lane to `R56_origin_append_only_trace_vm_semantics_gate` if exact.
