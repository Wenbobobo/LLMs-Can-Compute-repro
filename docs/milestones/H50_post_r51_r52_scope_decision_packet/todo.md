# H50 Todo

- interpret landed `R51` and `R52` explicitly;
- reclassify claim layers `A/B/C/D` as supported, unsupported, or disconfirmed
  on the bounded current substrate;
- decide whether to freeze as a narrow specialized executor, stop as exact
  without system value, or save `F27` as the only later planning entry; and
- keep broader trainable/transformed entry blocked unless the decision is
  explicit and bounded.
