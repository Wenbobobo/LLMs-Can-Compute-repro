# Acceptance

- `pointer_like_exact` stays exact where `linear_exact` is exact;
- at least one negative control fails in a claim-relevant way;
- the lane ends with one explicit mechanism verdict;
- no claim here exceeds the current same-endpoint runtime scope.
