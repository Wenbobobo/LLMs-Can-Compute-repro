# R20 D0 Runtime Mechanism Ablation Matrix

Planned mechanism lane after `R19`. `R20` separates "the runtime got faster"
from "the runtime got faster because latest-write retrieval was implemented in
the intended pointer-like way."
