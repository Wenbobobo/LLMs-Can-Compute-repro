# Todo

- Freeze this branch unless a future intervention can improve held-out
  structural or `opcode_shape` rollout without relying on opcode-legal collapse.
- Preserve the diagnostic `opcode_legal` artifact only as evidence about mask
  strength, not baseline learning.
- Reuse this branch later only if a publication comparison needs a final
  side-by-side pointer-space baseline table or matched-capacity ablation.
