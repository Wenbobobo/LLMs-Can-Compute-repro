# Artifact Index

- `results/M5_pointer_baseline/training_run.json`
  Final narrow repair export with structural baseline metrics plus
  `opcode_shape` and `opcode_legal` diagnostics on the matched harder slice.
