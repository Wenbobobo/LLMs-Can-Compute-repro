# Acceptance

- the current Origin-core value floor is separated from richer candidate value
  families;
- each richer family has explicit semantic obligations rather than vague
  "more expressive" wording;
- comparator requirements are explicit before any future semantic-boundary or
  frontier story is discussed;
- `F9` remains blocked and `F11` remains new-substrate after the matrix is
  written down;
- no runtime lane, frontend widening, or hybrid bridge is activated here.
