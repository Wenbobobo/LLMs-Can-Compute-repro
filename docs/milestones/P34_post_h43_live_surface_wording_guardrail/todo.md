# P34 Todo

- add a focused machine-readable guardrail for current live helper/control
  surfaces so future unattended runs catch stale “current compiled
  endpoint/boundary” regressions automatically;
- restrict the lint scope to current live control/helper surfaces rather than
  broad historical plans/results;
- permit bounded negative phrasing such as `not the whole current endpoint`
  while blocking affirmative stale endpoint wording;
- add a focused `P34` exporter plus tests/results for the live-surface wording
  guardrail packet; and
- record the completed auxiliary guardrail packet in status, plan, milestone,
  publication-record, and active-wave handoff surfaces without displacing
  `P31`.
