# Open Questions

These questions are non-blocking for the current plan.

- Are the next strongest streams best generated from longer existing programs or
  from new branch-heavy templates inside the current DSL?
- Does the most important next failure mode come from larger address magnitude,
  longer horizons, or more state reuse?
