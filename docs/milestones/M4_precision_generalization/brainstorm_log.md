# Brainstorm Log

- Rejected the idea of immediately adding more synthetic sweeps because the
  current gap is now about real-trace interpretation, not local geometry.
- Rejected expanding to richer attention schemes before the current precision
  failure modes are classified.
- Chosen direction: broaden the stream families first, then classify failure
  types, then update the claim boundary.
