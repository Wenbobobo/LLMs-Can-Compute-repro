# Blocked Hypotheses

- `R22` should not be used to widen beyond the current `D0` endpoint.
- A larger exact grid without a failure is still a useful bounded result.
- `R22` does not justify a repair lane unless a later refreeze says so
  explicitly.
