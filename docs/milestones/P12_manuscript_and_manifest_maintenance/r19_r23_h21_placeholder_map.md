# R19-R23 H21 Placeholder Map

## Purpose

This file reserves the post-`H19` packet placeholders needed for later paper
assembly without treating those placeholders as new claims.

## Main-text placeholders

- `R19` compact runtime-generalization row:
  admitted-plus-heldout exactness stays intact inside the same-endpoint
  envelope, with `pointer_like_exact` as the emphasized candidate.
- `R23` compact mixed systems gate row:
  exactness is preserved on `25/25`, speed improves versus imported
  accelerated, but the gate remains mixed because the best current reference
  path is still faster.
- `H21` frozen claim-partition note:
  `supported_here`, `unsupported_here`, and `disconfirmed_here` remain explicit
  after the `R22/R23` follow-up.

## Appendix placeholders

- `R20` mechanism-ablation table:
  compare `pointer_like_exact` against shuffled pointer-like and
  address-oblivious controls on the fixed `16`-row probe set.
- `R21/R22` paired boundary-map summary:
  present the bounded `96/96` and extended `102/102` no-break-observed scans as
  one limit-sharpening companion, not as localized failure evidence.
- `R19` runtime rows appendix pointer:
  keep admitted/heldout runtime matrices available without promoting every row
  into the main text.
- `R23` systems rows appendix pointer:
  keep runtime/component rows inspectable while the main text stays at the
  verdict and key timing anchors.

## Exclusions

- No placeholder here should imply a widened frontend or a broader compiled
  language target.
- No placeholder here should imply that `R22` already found the true executor
  boundary.
- No placeholder here should imply that `R23` overturned the mixed systems
  gate.
