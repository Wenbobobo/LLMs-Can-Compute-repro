# Post-H21 Manuscript Implications

## Purpose

This note records what the landed `R22/R23/H21` packet changes in the
manuscript-facing bundle without widening the scientific claim set.

## Main-text implications

- The systems section should now cite both the original mixed `R2` gate and the
  later same-endpoint `R23` recheck.
- `R23` should be written as a first-class mixed result:
  `pointer_like_exact` stayed exact on `25/25` full-suite rows, improved
  materially versus imported accelerated, and still remained about `4.16x`
  slower than the best current reference path.
- The compiled-boundary section should now treat the post-`H19` packet as part
  of the endpoint story rather than as downstream housekeeping:
  `R19` supports within-envelope runtime generalization, `R20` supports the
  mechanism through matched negative controls, `R21/R22` still do not localize
  a true executor boundary, and `H21` freezes that partition explicitly.
- The negative-results and threats sections should state directly that `R22`
  is no-break-observed rather than true boundary localization, and that `R23`
  remains mixed rather than near-positive.

## Appendix implications

- Keep `R19` as a compact runtime-generalization companion rather than a new
  headline result.
- Keep `R20` as a mechanism-ablation companion with exact negative controls.
- Keep `R21/R22` paired as boundary-map companions that sharpen the limit
  without claiming localization.
- Keep `R23` as a compact systems-gate companion table or row set.
- Keep `H21` as the frozen claim-partition note that routes future work back to
  conservative manuscript/manifest maintenance.

## Wording constraints

- Do not rewrite exactness preservation as systems closure.
- Do not rewrite larger exact grids as boundary localization.
- Do not let post-`H21` packet language imply widened frontend, compiled-
  language, or “LLMs are computers” scope.
- Treat mixed and negative evidence as claim-bearing rather than as temporary
  blockers that the draft expects to disappear.
