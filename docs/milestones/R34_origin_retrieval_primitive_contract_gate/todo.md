# R34 Todo

- export primitive rows for latest-write, stack top, stack depth, call/return,
  and tie-average geometry;
- preserve linear-oracle and accelerated results side by side;
- flag any primitive that only works with hidden side information as blocked;
- hand off to `R35_origin_append_only_stack_vm_execution_gate` if the primitive
  suite stays exact.
