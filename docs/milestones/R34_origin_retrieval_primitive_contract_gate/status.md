# R34 Status

- `R34` is the first runtime lane opened under `H28`.
- The lane is scoped to primitive retrieval contracts only.
- It does not widen to frontier demos, broad compiler claims, or general LLM
  narratives.
- The gate hands off directly to `R35` if the primitive suite stays exact.
