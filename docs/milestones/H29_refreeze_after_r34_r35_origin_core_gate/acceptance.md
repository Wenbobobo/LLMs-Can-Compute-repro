# H29 Acceptance

`H29` passes only if:

- it freezes both `R34` and `R35` explicitly;
- it records the active Origin-core A/B/C chain as positive, mixed, or blocked;
- it decides whether `R36` is justified next without reopening blocked
  same-endpoint or headline-narrative lanes by momentum;
- it preserves `R29`, `F3`, and `F2` discipline.
