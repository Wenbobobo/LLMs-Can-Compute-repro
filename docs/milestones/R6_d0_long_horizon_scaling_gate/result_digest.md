# Result Digest

`R6` exported the bounded long-horizon scaling gate on the current tiny
typed-bytecode `D0` endpoint.

## What `R6` closed

- kept all `24` fixed-multiplier rows admitted on the same current families,
  with `6` exact-trace matches, `18` exact-final-state matches, and `0`
  contradiction candidates across the full suite;
- made longer-row linear-versus-accelerated decode parity explicit on `8/8`
  family representatives, with `8/8` exact read-agreement matches;
- recorded concrete scaling growth over the preserved baseline seeds, reaching
  a maximum step/event growth of about `7.81x`;
- ran one narrow precision companion on the multiplier-`8` memory streams,
  where `4/8` candidate streams carried a boundary signal and the weaker
  control failed on `2/4` of those boundary-bearing rows;
- kept `E1c` inactive and routed the mainline to `R7`.

## What `R6` did not do

- did not widen frontend, language, or systems scope;
- did not convert the precision companion into a broad robustness claim;
- did not reopen `R5` or authorize a broader “LLMs are computers” reading.
