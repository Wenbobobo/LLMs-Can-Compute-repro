# Status

Opened and completed on 2026-03-19.

- `R6` exported a fixed `{2,4,8}` long-horizon scaling grid over `8` current
  scalable `D0` families, producing `24` exactness rows without widening the
  endpoint semantics;
- all `24/24` rows remained admitted with `0` contradiction candidates,
  `8/8` longer-row linear-versus-accelerated decode parity matches, and a
  maximum step/event growth of about `7.81x` versus preserved baseline seeds;
- the narrow precision companion stayed bounded to multiplier-`8` memory
  rows, found `4` boundary-bearing streams out of `8` candidates, and the
  weaker control failed on `2/4` of those boundary-bearing rows;
- `E1c` remained inactive and the next live lane stayed
  `R7_d0_same_endpoint_runtime_bridge`.
