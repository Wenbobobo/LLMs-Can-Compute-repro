# P36 Todo

- record one clean worktree strategy for the post-`H49` wave;
- record one dirty-root `main` quarantine note;
- record one artifact policy separating compact summaries from raw dumps;
- record one commit cadence for `F26`, `R51`, `R52`, and `H50`;
- keep merge posture explicit and non-executed here; and
- keep LFS inactive by default unless a later explicit review-critical artifact
  cannot stay compact.
