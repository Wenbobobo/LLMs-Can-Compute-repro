# P36 Status

- completed operational/docs hygiene packet after landed `H49`;
- preserves `H49` as the current active docs-only packet;
- preserves `H43` as the current paper-grade endpoint;
- promotes the clean post-`H49` worktree as the only scientific execution
  surface for this wave;
- quarantines dirty root `main` from scientific execution;
- keeps `merge_executed = false` explicit and keeps `main` untouched during
  `F26/R51/R52/H50`;
- codifies compact-summary-in-git and raw-dump-out-of-git defaults; and
- records the commit cadence for planning, runtime, comparator, and decision
  packets.
