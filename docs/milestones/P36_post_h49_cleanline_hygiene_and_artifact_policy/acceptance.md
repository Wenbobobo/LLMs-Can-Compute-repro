# P36 Acceptance

- `P36` remains operational/docs-only;
- `H49` remains the current active docs-only packet;
- `H43` remains the current paper-grade endpoint;
- the clean post-`H49` worktree is the only scientific execution surface for
  this wave;
- dirty root `main` remains quarantined from scientific execution;
- raw probe/per-read dumps stay out of git by default;
- compact summaries, manifests, stop rules, and first-fail digests stay in git;
- merge back to `main` does not occur during `F26/R51/R52/H50`; and
- any future LFS use remains opt-in and explicit rather than default policy.
