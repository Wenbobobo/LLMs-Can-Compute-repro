# P36 Post-H49 Cleanline Hygiene And Artifact Policy

Completed operational/docs hygiene packet for the post-`H49` wave.

`P36` does not alter the active scientific stage. It preserves `H49` as the
current active docs-only packet, preserves `H43` as the paper-grade endpoint,
preserves the clean post-`H49` worktree as the only scientific execution
surface for this wave, and codifies the artifact/commit policy needed for
`F26 -> R51 -> R52 -> H50`.

The packet fixes:

- one worktree strategy note;
- one artifact policy note;
- one commit-cadence note;
- one acceptance note keeping `main` untouched during this wave; and
- one explicit split between compact in-git summaries and raw out-of-git
  dumps.
