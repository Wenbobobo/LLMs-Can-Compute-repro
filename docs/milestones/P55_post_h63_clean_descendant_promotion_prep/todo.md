# TODO

- keep promotion-prep separate from merge execution
- keep next handoff docs synchronized with the active `H64` stack
- keep downstream planning anchored on archive / hygiene / stop by default
