# F25 Todo

- save the dedicated `F25` design;
- define the smallest admitted restricted tiny-`C` lowering surface on top of
  the preserved useful-case kernels;
- define explicit non-admitted features and kill criteria before execution;
- keep heap, recursion, floats, IO, and arbitrary `C` out of scope unless a
  later explicit packet changes the boundary.
