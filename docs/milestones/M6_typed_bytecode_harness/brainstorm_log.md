# Brainstorm Log

- Keep the first frontend brutally narrow.
- Keep type distinctions visible to the verifier even when they erase to the
  same runtime opcode.
- Prefer one frozen harness schema over ad hoc per-program result summaries.
