# Experiment Matrix

- Inputs:
  current DSL semantics,
  replay rules,
  frozen bytecode type system,
  frozen opcode/lowering contract.
- Outputs:
  verifier result rows,
  lowering equivalence rows,
  differential harness rows for short exact-trace and long exact-final-state
  programs.
