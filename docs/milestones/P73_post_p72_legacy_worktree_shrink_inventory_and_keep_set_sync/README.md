# P73 Post-P72 Legacy Worktree Shrink Inventory And Keep Set Sync

Operational sidecar that inventories the legacy local worktree footprint after
`P72`, re-anchors the live keep-set on `D:/zWenbo/AI/wt`, and prepares later
safe local shrink plus dossier-only merge-prep follow-through.
