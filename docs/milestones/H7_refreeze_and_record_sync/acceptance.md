# Acceptance

- the repo returns to one narrow active surface after the current packet;
- standing audits are green on the latest active-driver wording;
- the repo stays bounded to the same `D0` endpoint.
