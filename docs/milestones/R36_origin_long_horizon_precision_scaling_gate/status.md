# R36 Status

- executed as a narrow precision follow-up under `H29`;
- sharpens where float32 `single_head` still collapses on the active
  Origin-core bundle;
- keeps decomposition evidence explicit and bounded;
- does not claim broad robustness beyond the exported suite.
