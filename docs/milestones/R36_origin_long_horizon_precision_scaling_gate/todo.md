# R36 Todo

- export one narrow screening ledger over high-address memory, deep stack, and
  call/control streams;
- record first failing horizon multipliers for float32 `single_head`;
- record whether `radix2` and `block_recentered` stay exact on the same rows;
- hand off to a later explicit packet rather than widening scope by momentum.
