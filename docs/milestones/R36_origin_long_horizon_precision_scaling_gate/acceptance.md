# R36 Acceptance

`R36` passes only if:

- at least one inflated-horizon float32 `single_head` failure is made explicit;
- at least one decomposition scheme stays exact on the same exported rows;
- the lane stays on the active Origin-core bundle and does not widen to a broad
  precision sweep campaign;
- results remain machine-readable at row level.
