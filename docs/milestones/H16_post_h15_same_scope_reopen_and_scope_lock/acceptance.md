# Acceptance

- `H16` is the canonical active driver everywhere that matters;
- `H15`, `H14`, `H13/V1`, and earlier same-endpoint baselines remain preserved;
- the reopened lane is explicitly limited to same-scope `D0` evidence;
- frontier recheck remains blocked until an explicit later decision.
