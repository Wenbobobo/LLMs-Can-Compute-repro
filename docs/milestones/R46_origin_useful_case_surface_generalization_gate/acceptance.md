# R46 Acceptance

- the held-out surface is fixed mechanically before execution;
- all admitted variants stay inside the landed restricted useful-case surface;
- source/spec, source/lowered, and free-running exact execution agree on every
  executed variant;
- the gate returns `surface_generalizes_narrowly` only if all `8/8` held-out
  variants remain exact;
- any broader surface, frontend, or model bridge still requires a later
  explicit packet after `R46`.
