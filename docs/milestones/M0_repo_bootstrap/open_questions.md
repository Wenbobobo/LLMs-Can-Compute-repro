# Open Questions

- Whether the current shell tooling can support `git` and `gh` in this session.
- Whether the first public checkpoint should include code only or code plus
  initial benchmark outputs.
