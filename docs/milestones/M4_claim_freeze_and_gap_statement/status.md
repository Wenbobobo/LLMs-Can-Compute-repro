# Status

Ready for downstream use.

Current freeze summary:
- `C2f` stays in the paper as a bridge result with an explicit decode-legality
  caveat;
- `C2h` is now a cleaned negative closure, not an invitation to invent a new
  intermediate regime;
- `C3e` is a broadened but still narrow precision boundary, not a general
  long-horizon robustness claim;
- `M6` remains gated to typed-bytecode spec work.
