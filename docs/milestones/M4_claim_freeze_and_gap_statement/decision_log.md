# Decision Log

- Freeze `C2f` as a qualified bridge result, not a broad executor claim.
- Freeze `C2h` as a negative closure because the cleaned support ratio
  (`0.5333`) is below the threshold for adding a fourth regime.
- Freeze `C3e` as a broadened current-suite boundary, not a general robustness
  theorem.
- Keep `M5` as a frozen negative control and `M6` as spec-only.
