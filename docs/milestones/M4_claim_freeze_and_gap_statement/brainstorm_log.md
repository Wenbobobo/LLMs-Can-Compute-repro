# Brainstorm Log

- Keep paper wording stricter than blog wording.
- Split the staged story into bridge (`C2f`) and gap closure (`C2h`) instead of
  forcing one mixed claim.
- Keep precision boundary language empirical: high-address memory is the sharp
  failure surface; stack depth `256` is harder than stack depth `64`, but not a
  total collapse.
