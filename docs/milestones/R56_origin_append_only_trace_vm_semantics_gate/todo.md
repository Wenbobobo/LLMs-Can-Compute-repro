# R56 Todo

- [x] define one bounded append-only trace-VM instruction set and state
  surface;
- [x] define one transparent reference semantics implementation for that
  contract;
- [x] execute the candidate internal trace-VM route free-running on the same
  rows;
- [x] export full step traces, final states, and first-fail localization;
- [x] reject teacher-forced or answer-only success as insufficient; and
- [x] stop the mechanism lane immediately if step-trace exactness fails or
  hand the lane to `R57_origin_accelerated_trace_vm_comparator_gate` if
  exact.
