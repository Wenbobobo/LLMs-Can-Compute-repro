# H49 Todo

- [x] interpret landed `R50` explicitly rather than widening by momentum;
- [x] choose exactly one of the two saved `R50` outcomes;
- [x] preserve `H48` as the prior docs-only packet and `H43` as the
  paper-grade endpoint;
- [x] record `R50` as completed evidence rather than an active lane;
- [x] restore `no_active_downstream_runtime_lane` after the decision packet;
- [x] keep broader Wasm/`C`, arbitrary `C`, and multi-function or
  dynamic-surface growth non-active unless a later explicit packet justifies
  them.
