# F16 Acceptance

- the bundle remains planning-only;
- each candidate records one status enum and one reason;
- at most one candidate may be `execution_ready`;
- the current bundle ends with zero `execution_ready` candidates;
- no new opcode, value family, row family, or substrate is introduced.
