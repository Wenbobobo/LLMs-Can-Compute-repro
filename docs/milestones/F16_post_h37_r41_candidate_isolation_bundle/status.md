# F16 Status

- completed planning-only candidate-isolation bundle after `H37`;
- assigns `nonunique` to
  `helper_annotation_ablation_or_canonicalization`;
- assigns `nonunique` to
  `control_surface_neutralization_without_semantic_change`;
- assigns `inadmissible` to
  `retrieval_critical_vs_local_easy_step_contrast_slicing` because no
  mechanical same-row slice pair is yet locked on both landed `R40` rows;
- produces `no_candidate_ready`, so `H38` must keep the `H36` freeze.
