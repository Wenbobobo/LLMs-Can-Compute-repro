# Status

Activated on 2026-03-21 after `H19` landed.

- `H19_refreeze_and_next_scope_decision` remains the current frozen scientific
  input;
- the repo is still operationally dirty, so `H20` exists to isolate prior-wave
  closeout, next-wave runtime science, and downstream public-surface sync;
- this stage must not widen the endpoint, activate frontier review, or mutate
  outward-facing root/publication wording ahead of new evidence;
- downstream science should only start from one explicit `H20` split summary.
