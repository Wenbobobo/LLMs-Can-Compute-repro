# P31 Todo

- refresh `blog_outline.md` so it treats `H43` as the current paper-grade
  endpoint and `H32/H34` as preserved earlier compiled-boundary support only;
- refresh `blog_release_rules.md` so blocked-blog guardrails keep `R43/R44/R45`
  explicit and stop treating preserved `D0` as the whole current endpoint;
- refresh manuscript/caption/appendix helper docs so preserved first-step
  `D0` language no longer overrides the broader current `H43` paper endpoint;
- refresh the contradictory current-wave block in `STATUS.md` and
  `docs/publication_record/README.md`, plus any stale summary helper wording in
  `release_summary_draft.md`, so they record `P31` as the current low-priority
  helper-doc wave;
- record the wave in plan, milestone, publication-record, manifest, and
  active-wave handoff surfaces; and
- export a focused `P31` summary/checklist/snapshot packet for the refreshed
  blocked-blog guardrails.
