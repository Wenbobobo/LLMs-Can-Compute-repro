# H34 Todo

- keep the two-outcome fork explicit:
  `freeze_compiled_boundary_as_complete_for_now` versus
  `authorize_one_more_origin_core_substrate_question`;
- keep the freeze rationale explicit:
  one declared permutation surviving is still not broad control-surface or
  broader compiler support;
- keep `H32` explicit as the current active routing packet throughout;
- require a new contradiction- or sharper-gap-driven explicit packet before
  any future same-substrate reopen;
- preserve `R29`, `F3`, arbitrary-language wording, and broader
  compiler/demo/frontier claims as blocked by default.
