# Blocked Hypotheses

- Do not widen the opcode set just to make the stress family look more like a
  public demo.
- Do not add recursion, dynamic call targets, heap allocation, or syscalls.
- Do not treat golden traces alone as an external reference oracle.
