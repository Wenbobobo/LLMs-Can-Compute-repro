# Artifact Index

- `results/M4_staged_pointer_decoder/summary.json`
  Main staged-pointer export with train/eval metrics plus structural,
  `opcode_shape`, and `opcode_legal` rollout on the harder held-out slice.
