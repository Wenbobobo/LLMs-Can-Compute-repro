# Research Notes

- `H2` does not validate science; it validates governance around the frozen
  science.
- The intent is to make unattended runs safer by forcing conflicts into
  explicit `E1` triggers instead of prose drift.
