# Acceptance

- the repo has one explicit post-`P7` bundle-lock audit with zero blocked
  items on the current state;
- publication-facing release-control docs are synchronized;
- future unattended runs can distinguish paper/release hygiene from real
  evidence reopen conditions.
