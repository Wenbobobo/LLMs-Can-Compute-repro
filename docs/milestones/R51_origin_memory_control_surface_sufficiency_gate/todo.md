# R51 Todo

- execute one bounded memory/control surface matrix above landed `R50`;
- compare source/interpreter, lowered replay/reference, and accelerated
  free-running exact execution;
- export full-trace parity, final-state parity, maximizer-row identity,
  first-fail localization, and annotation-budget accounting;
- stop on hidden mutable side channels, exactness loss, or uncontrolled
  surface drift; and
- hand the result to `H50` without opening any broader lane directly.
