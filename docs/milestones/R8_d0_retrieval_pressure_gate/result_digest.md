# Result Digest

`R8` exported the bounded same-endpoint retrieval-pressure gate on the current
tiny typed-bytecode `D0` endpoint and remained positive without widening
scope.

## What `R8` closed

- raised retrieval pressure on one `10x` harder row from each of the top `4`
  direct-baseline families while keeping the endpoint fixed;
- kept all `4/4` harder-family exact-suite rows admitted, with `0`
  harness/annotation gaps and `0` contradiction candidates;
- kept a bounded uniform-load decode-parity probe explicit on the top `2`
  heaviest admitted rows, where `2/2` rows matched exactly;
- exported quantitative pressure growth, reaching about `1.249x` maximum event
  growth and about `1.560x` maximum total candidate-depth growth versus the
  admitted `R6` `8x` source rows;
- closed with `go_harder_retrieval_pressure_exact`, left `E1c` inactive, and
  handed the packet forward to `R9` and `R10`.

## What `R8` did not do

- did not widen semantics, frontend scope, or arbitrary compiled-language
  claims;
- did not reopen the broader systems lane;
- did not claim exhaustive parity on every heavier-row read; parity remains a
  bounded probe so the unattended gate stays executable.
