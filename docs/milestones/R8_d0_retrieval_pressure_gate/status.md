# Status

Opened on 2026-03-20.
Closed on 2026-03-20 with `go_harder_retrieval_pressure_exact`.

- `R8` stayed on harder same-endpoint `D0` families only;
- the exact suite closed with `4/4` admitted rows and `0` contradiction candidates;
- the bounded uniform-load decode-parity probe closed with `2/2` admitted rows and `0` mismatches;
- retrieval pressure reached about `1.249x` max event growth and `1.560x` max total candidate-depth growth versus the admitted `R6` `8x` source rows;
- `E1c` stayed inactive and the packet now hands off to `R9/R10`.
