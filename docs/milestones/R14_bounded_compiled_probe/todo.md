# Todo

- [x] Wait for `R11/R12` closure before deciding whether `R14` is justified.
- current decision: `R14` is not currently justified because no compiled
  follow-up need is exposed by the reopened deterministic lane so far.
- [ ] If justified, define the smallest useful compiled probe on `D0`.
- [ ] Keep claim language bounded to the same frozen endpoint.
