# R37 Todo

- keep the admitted source subset fixed to straight-line arithmetic, one branch
  pattern, one bounded loop, and one single-helper call row;
- export source/reference, lowering, and free-running exactness rows
  separately;
- keep exact trace and exact final state as separate pass criteria;
- reject any interpretation that widens the result into arbitrary language or
  same-endpoint systems claims.
