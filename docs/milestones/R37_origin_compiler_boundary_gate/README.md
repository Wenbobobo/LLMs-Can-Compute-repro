# R37 Origin Compiler Boundary Gate

Executed narrow compiled/lowered boundary gate after `H29/R36`.

`R37` tests one tiny admitted bytecode subset on the current Origin-core
substrate without reopening same-endpoint systems recovery or broad
compiler/demo rhetoric. The lane keeps the source surface fixed to:

- straight-line integer arithmetic;
- one equality-branch pattern (true and false rows);
- one bounded loop with static locals;
- one optional single-helper call that stays inside the already validated
  `CALL/RET` surface.
