# Status

Opened and completed on 2026-03-19.

- `R3` exported one bounded harder-suite exact-execution gate on the same
  tiny typed-bytecode `D0` endpoint;
- all `7` admitted rows stayed exact under bytecode, lowered `exec_trace`,
  and standalone spec agreement;
- linear versus accelerated Hull decode parity stayed exact on all admitted
  lowered rows;
- `4` longer memory streams entered the immediate float32 precision companion
  screen, while `E1c` stayed inactive.
