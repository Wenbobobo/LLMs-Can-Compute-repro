# Result Digest

`R3` exported a bounded harder-suite exact-execution gate on the current `D0`
endpoint.

## What `R3` closed

- kept all `7` admitted rows exact under bytecode, lowered `exec_trace`, and
  standalone spec agreement;
- made linear versus accelerated Hull decode parity explicit on all admitted
  lowered rows, with `7/7` parity matches;
- ran one narrow precision companion on the longer positive rows only, with
  `4` memory streams entering the float32 boundary screen and one weaker
  base-`256` control failing on `3/4` of those boundary-bearing streams;
- left `E1c` inactive because no frozen-`D0` contradiction appeared.

## What `R3` did not do

- did not authorize frontend widening or arbitrary compiled-language claims;
- did not convert the immediate precision companion into a broad long-horizon
  robustness claim;
- did not activate `R5`; the next live lane is `R4`.
