# Acceptance

- the new suite stays bounded to the same `D0` endpoint;
- exact-trace and exact-final-state outcomes are explicit for each row;
- boundary failures are typed rather than narrated away;
- only true frozen-`D0` contradictions may route to `E1c`.
