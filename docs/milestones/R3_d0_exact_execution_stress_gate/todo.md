# Todo

- [x] Define the bounded `R3` stress suite on the existing `D0` endpoint.
- [x] Export exact-trace, exact-final-state, first-mismatch, and failure-reason rows.
- [x] Keep `linear` and `Hull` decode parity explicit on all admitted rows.
- [x] Run narrow precision follow-up only on newly boundary-bearing rows.
- [x] Add one weaker negative/control comparison if it clarifies the active boundary.
- [x] Record the contradiction-routing rule for `E1c` and close `R3`.
