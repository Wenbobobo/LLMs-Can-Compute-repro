# Research Notes

- The main question is whether exact execution itself survives harder traces,
  not whether another narrative or demo can be assembled.
- `R3` should prefer fewer, sharper suites over many weakly justified ones.
- The longer memory streams are the only rows that immediately re-enter the
  finite-precision conversation here; stack streams stayed easier on the
  screened horizon grid.
- The active interpretation of any `R3` failure remains typed: contradiction
  first, bounded precision companion second, unsupported widening never.
