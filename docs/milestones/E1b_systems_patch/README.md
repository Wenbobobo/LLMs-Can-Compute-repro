# E1b Systems Patch

Goal: deliver one bounded systems patch bundle on current `R2` / `D0` scope.

Scope:

- add component-attribution views from existing runtime profile rows;
- bridge geometry asymptotic wins to same-scope runtime gap accounting;
- keep gate wording bounded and mixed unless evidence changes on current scope.

Out of scope:

- no frontend widening;
- no arbitrary-C/runtime generalization;
- no shortcut from asymptotic win to broad superiority claim.

