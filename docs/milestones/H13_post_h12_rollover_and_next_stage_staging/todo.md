# Todo

- [x] Preserve the completed `H10/H11/R8/R9/R10/H12` packet as the latest
  refrozen same-endpoint baseline in the next driver transition.
- [x] Replace stale root or publication wording that still treated `H12` as an
  unfinished active closeout.
- [x] Route the unresolved full-suite `pytest -q` runtime issue into `V1`
  instead of leaving it implicit.
- [x] Close one bounded per-file timing follow-up so `V1` leaves an explicit
  operational disposition rather than a generic "needs timing" note.
- [x] Leave one explicit post-`H12` execution order for the next unattended
  wave.
- [x] Hand control forward to `H14_core_first_reopen_and_scope_lock`.
