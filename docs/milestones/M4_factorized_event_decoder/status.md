# Status

- Implemented shared `FactorizedEventCodec` and event-history transformer.
- Added the first direct event-value export in
  `results/M4_factorized_event_decoder/summary.json`.
- Current milestone state: in progress, with the main gap now explicitly
  measured rather than inferred.
