# F22 Acceptance

- the bundle remains planning-only and comparator-only;
- `H46` remains the current active docs-only decision packet;
- `H45` remains the preserved prior docs-only decision packet;
- `H43` remains visible as the current paper-grade endpoint;
- exact `R47` and explicit `H46` remain visible as the satisfied prerequisites
  for `R48`;
- `R48` is the now-authorized next comparator-only model lane;
- model evidence remains comparator-only and non-substitutive relative to exact
  evidence; and
- claim ceilings do not move beyond bounded useful cases here.
