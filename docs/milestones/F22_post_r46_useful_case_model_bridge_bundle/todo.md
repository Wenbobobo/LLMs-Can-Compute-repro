# F22 Todo

- keep `F22` explicit as the current comparator-planning bundle after `H46`;
- keep exact `R47` and executed `H46` explicit as the landed prerequisites for
  `R48`;
- keep `compiled_weight_executor` and `trainable_2d_executor` subordinate to
  the landed exact useful-case contract rather than substituting for it; and
- keep first-error position, failure class, broader Wasm/C, broader hybrid
  model work, and merge-to-`main` explicit and non-active here.
