# F22 Status

- current comparator-only planning bundle after completed `H46`;
- preserves `H46` as the current active docs-only decision packet;
- preserves `H45` as the preserved prior docs-only decision packet;
- preserves `H44` as the preserved prior route reauthorization packet;
- preserves `H43` as the current paper-grade endpoint;
- supports the now-authorized
  `R48_origin_dual_mode_useful_case_model_gate`;
- keeps model evidence downstream of exact frontend evidence and does not let
  model positives replace exact failures;
- does not authorize broader Wasm/C or broader hybrid model work by momentum;
- keeps `merge_executed = false`.
