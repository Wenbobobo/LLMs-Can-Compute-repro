# H30 Todo

- freeze `H29`, `R36`, and `R37` into one explicit machine-readable packet;
- keep supported, unsupported, and disconfirmed claims explicit;
- preserve blocked lanes and the no-momentum rule;
- hand clean-worktree closeout to `P17`;
- require a later explicit packet before any compiled-boundary extension or
  language-level scope lift.
