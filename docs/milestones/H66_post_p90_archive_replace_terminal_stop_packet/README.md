# H66 Post-P90 Archive Replace Terminal Stop Packet

Docs-only packet that promotes the completed `P90` archive/replace screen into
the current terminal-stop route.

`H66` preserves `H65` as the prior active packet, keeps runtime closed, and
defaults the next route to explicit stop, archive polish, or no further
action.
