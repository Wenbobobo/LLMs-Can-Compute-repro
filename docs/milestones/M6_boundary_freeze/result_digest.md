# Result Digest

- Current `D0` evidence:
  22 verifier-passing rows,
  7 deterministic verifier failures,
  16 short/medium exact-trace matches,
  6 long exact-final-state matches.
- Current diagnostic companion:
  6 annotated memory-surface rows,
  2 deterministic negative controls,
  `6/6` reference-vs-lowered surface matches.
- Intended conclusion:
  freeze this slice narrowly, then pressure it with one serious stress family
  and one external reference comparison before any broader demo work.
- Decision note:
  the current slice is frozen as the last pre-demo widening unless one of the
  explicit contradiction triggers fires.
