# P33 Todo

- refresh `docs/publication_record/e1_patch_playbook_matrix.md` so dormant
  `E1b/E1c` routing language describes preserved same-endpoint `R2/D0` scope
  honestly relative to the broader current `H43` endpoint;
- refresh `docs/publication_record/e1b_systems_patch_playbook.md` so the lane
  is framed as preserved same-endpoint systems support rather than current
  scope;
- refresh `docs/publication_record/e1c_compiled_boundary_patch_playbook.md`
  so the lane is framed as preserved first-step `D0` compiled-boundary support
  rather than the whole current endpoint;
- refresh `docs/publication_record/layout_decision_log.md` plus selected
  historical rows in `docs/publication_record/experiment_manifest.md` so older
  helper wording no longer elevates preserved `D0/R2` scope into the live
  paper endpoint;
- add a focused `P33` exporter plus tests/results for the dormant-playbook
  wording refresh packet; and
- record the completed auxiliary packet in status, plan, milestone,
  publication-record, and active-wave handoff surfaces without displacing
  `P31`.
