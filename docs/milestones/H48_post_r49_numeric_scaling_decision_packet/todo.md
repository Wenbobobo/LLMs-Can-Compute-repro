# H48 Todo

- [x] interpret landed `R49` explicitly rather than widening by momentum;
- [x] choose exactly one of the two saved `F23` outcomes;
- [x] preserve `H47` as the prior docs-only packet and `H43` as the
  paper-grade endpoint;
- [x] authorize only a planning bundle, not a new runtime lane;
- [x] keep `P36` explicit as the non-selected falsification-closeout route;
- [ ] save the dedicated `F25` planning bundle;
- [ ] keep broader Wasm/`C`, arbitrary `C`, and hybrid growth non-active
  unless a later explicit packet justifies them.
