# Todo

Legacy note: the remaining unchecked rows are tracked in
`docs/milestones/H1_legacy_backlog_reconciliation/classification_matrix.md`
and are not active on the current frozen paper scope by default.

- [x] Build one unified summary of native horizon, first-failure multiplier,
  and failure type across the current real/organic suites.
- Follow-up moved to `R3_d0_exact_execution_stress_gate`: continue narrow
  horizon/base sweeps only where they sharpen the boundary story for an
  already-exported or newly admitted active `D0` family.
- Follow-up moved to `R3_d0_exact_execution_stress_gate`: add targeted
  negative/control comparisons only when they clarify the currently active
  tie-collapse mechanism rather than widening the suite.
- [x] Distill the decomposition story into “helps here / unproven here /
  unsupported here”.
- [x] Feed the final boundary statement back into `P3`.
