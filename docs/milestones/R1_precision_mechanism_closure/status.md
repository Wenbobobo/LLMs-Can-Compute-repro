# Status

Provisioned on 2026-03-19 from the existing `M4` precision bundles.

- current evidence already shows single-head float32 failure on both synthetic
  and real/organic traces;
- decomposition schemes remain promising on the exported suite, but the final
  closure still needs one coherent mechanism statement;
- this milestone exists to close wording and boundary logic, not to restart an
  open-ended sweep program.
- the first closure bundle now exists under
  `results/R1_precision_mechanism_closure/`;
- current summary reports single-head failure on `12/25` tracked streams, with
  `7` failing immediately at `1x`, while at least one decomposition
  configuration remains fully passing on all currently tracked streams.
