# R1 Precision Mechanism Closure

Goal: finish the mechanistic story for finite-precision latest-write retrieval
on real and organic traces.

Scope:

- unify current `C3d` and `C3e` evidence into one precise boundary statement;
- continue only the narrow sweeps that change the mechanism story;
- preserve exact failure-type accounting and negative controls.

Out of scope:

- new retrieval families unless they resolve a concrete ambiguity;
- broad scale claims beyond the validated suite.
