# Acceptance

- each tracked family has a native horizon, first-failure point, and failure
  type;
- the repo can state exactly where single-head fails and where current
  decomposition helps;
- any remaining unsupported extrapolation is written down explicitly;
- `P3` can cite a single coherent `C3e` boundary story.
