# F18 Status

- completed planning-only long-arc bundle after `H38`;
- fixes the current claim ladder as `A/B/C/D` rather than headline rhetoric;
- makes `F9` the default forward planning family while keeping it inactive;
- keeps `R41` deferred as a contradiction route only;
- records a save-first, clean-worktree-first, no-merge execution policy.
