# F18 Acceptance

- `H38` remains the active docs-only decision packet;
- `H36` remains the active routing/refreeze packet underneath it;
- `F9` is named explicitly as the preferred forward planning family;
- `R41` remains deferred and non-default;
- `F11` remains explicitly outside the current substrate;
- no runtime lane or merge authorization is created by `F18`.
