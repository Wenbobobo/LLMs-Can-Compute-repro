# F18 Post-H38 Origin-Core Long-Arc Bundle

Planning-only long-arc bundle after the completed
`F16 -> H38 -> P26 -> F17` control wave.

`F18` exists to lock the post-`H38` scientific target, preferred route, and
execution hygiene rules into one decision-complete planning surface. It does
not authorize runtime execution or replace `H38` as the active packet.

This bundle preserves:

- `H38` as the current active docs-only decision packet;
- `H36` as the underlying active routing/refreeze packet;
- `P26` as the completed operational audit lane with `audit_only` posture;
- `F17` as the route-selection bundle that blocks automatic reopen;
- `R41` as a deferred contradiction route only;
- `F9` as the preferred semantic-boundary family for later forward progress;
- `F11` as a new-substrate family, not a current follow-on;
- `no active downstream runtime lane`.
