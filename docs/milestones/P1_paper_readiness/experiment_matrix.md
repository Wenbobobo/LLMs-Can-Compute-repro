# Experiment Matrix

- Inputs:
  all milestone artifacts,
  top-level claim docs,
  runtime environment export.
- Outputs:
  synchronized ledgers,
  mandatory figure/table status,
  claim-impact deltas by unattended batch.
