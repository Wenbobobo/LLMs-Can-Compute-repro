# Decision Log

- Paper first; blog second.
- Every unattended batch must update ledgers before it is considered complete.
- Mandatory figures/tables are fixed before any draft sprint starts.
- Chosen immediate output is the ledger status trio (`summary.json`, `figure_table_status.json`, `claim_bundle_completeness.json`) so future authors can trace the first bundle before new figures are sketched.
