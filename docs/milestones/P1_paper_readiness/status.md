# Status

Ongoing documentation track.

- Ledgers now include `M4_failure_provenance`, `M4_precision_organic_traces`,
  and `M6_typed_bytecode_harness`.
- `docs/publication_record/paper_bundle_status.md` now tracks mandatory figure
  and table readiness explicitly.
- The first paper-bundle status layer now exists in
  `results/P1_paper_readiness/summary.json`, with companion exports
  `figure_table_status.json` and `claim_bundle_completeness.json`.
- Canonical paper-source exports now also exist for the `M4` failure taxonomy,
  the `M4` real-trace precision boundary, and the initial `M6` exact-trace /
  final-state table.
- The same `D0` slice now also has a synced appendix-level memory-surface
  diagnostic summary and markdown table under `results/P1_paper_readiness/`.
- The remaining layout-only blockers in the current bundle are now rendered as
  SVG figures, and the initial `M6` exact-trace / final-state rows now also
  have a markdown table layout.
