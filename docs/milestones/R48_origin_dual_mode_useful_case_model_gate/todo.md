# R48 Todo

- evaluate `compiled_weight_executor` and `trainable_2d_executor` on the
  preserved useful-case contract only;
- keep the held-out useful-case family explicit and fixed rather than widening
  the evidence contract;
- keep exact evidence decisive and non-substitutive relative to model
  positives;
- export first-error position, failure class, exact row counts, and per-mode
  rollups; and
- keep broader Wasm/C, broader hybrid model work, and merge-to-`main`
  non-active here.
