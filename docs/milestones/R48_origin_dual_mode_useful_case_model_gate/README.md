# R48 Origin Dual-Mode Useful-Case Model Gate

Authorized next comparator-only model gate after exact `R47` and executed
`H46`.

`R48` will compare `compiled_weight_executor` and `trainable_2d_executor`
against the preserved exact useful-case contract. Exact evidence remains
decisive, model positives do not replace exact failures, and first-error
position plus failure class are mandatory outputs here.
