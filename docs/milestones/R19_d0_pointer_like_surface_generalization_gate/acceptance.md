# Acceptance

- the original admitted `8/8` rows remain exact;
- held-out rows are either exact or explicitly logged as failures;
- the lane ends with one explicit generalization verdict;
- no sample leaves the fixed `D0` endpoint.
