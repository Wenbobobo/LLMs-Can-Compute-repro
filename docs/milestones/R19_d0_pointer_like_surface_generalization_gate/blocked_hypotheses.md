# Blocked Hypotheses

- `R19` should not assume that one strong focused `R18b` result automatically
  generalizes to every admitted family.
- `R19` should not turn a held-out failure into an on-the-fly runtime repair
  exercise.
- `R19` should not redefine the admitted envelope after seeing failures.
