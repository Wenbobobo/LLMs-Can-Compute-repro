# Todo

- [x] Build the held-out family and seed manifest from the admitted `R17`
  surface.
- [x] Compare `linear_exact`, `R17` accelerated, and `pointer_like_exact` on
  the original admitted rows.
- [x] Run the same comparison on the held-out rows that stay inside the
  declared same-endpoint envelope.
- [x] Export row-level exactness, runtime ratios, and address profiles.
- [x] Record a single lane verdict without broadening the claim boundary.
