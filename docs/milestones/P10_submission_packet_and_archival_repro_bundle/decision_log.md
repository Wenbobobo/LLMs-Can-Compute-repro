# Decision Log

- Use one packet index plus one archival manifest instead of scattering the
  handoff across many existing ledger files.
- Keep the packet downstream of the locked manuscript bundle.
- Require a machine-readable audit before calling the packet ready.
