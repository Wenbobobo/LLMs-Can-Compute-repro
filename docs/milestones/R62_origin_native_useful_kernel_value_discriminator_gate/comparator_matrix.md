Compared routes:

- native linear internal trace-VM execution
- native accelerated internal trace-VM execution
- transparent external trace interpreter execution
- plain external scalar reference runtime

Timing rule:

- native program-construction time is exported separately and not folded into
  the measured executor comparator.
