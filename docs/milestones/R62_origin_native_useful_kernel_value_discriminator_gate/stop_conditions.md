Close to `H58` immediately if:

- any declared row breaks exactness
- longest rows remain slower under accelerated than under linear
- accelerated stays outside one order of magnitude of the external scalar
  comparator on every kernel
