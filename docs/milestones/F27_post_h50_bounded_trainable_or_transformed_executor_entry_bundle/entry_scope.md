# F27 Entry Scope

If `H50` is positive, `F27` may define exactly one bounded next planning
surface:

- one trainable-executor entry route; or
- one transformed/compiled-executor entry route.

It may not define both at once without a later explicit packet.
