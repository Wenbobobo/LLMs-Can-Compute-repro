# F19 Status

- completed planning-only semantic-boundary roadmap after `F18`;
- fixes one bounded restricted-Wasm / tiny-`C` surface;
- fixes `R42`, `R43`, and `R44` as the next deferred future gate family;
- fixes the three-kernel useful-case ladder:
  `sum_i32_buffer`,
  `count_nonzero_i32_buffer`,
  `histogram16_u8`;
- keeps exact lowering / compile-style execution as the default first route.
