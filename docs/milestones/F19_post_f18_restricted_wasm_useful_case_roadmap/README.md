# F19 Post-F18 Restricted Wasm Useful-Case Roadmap

Planning-only semantic-boundary roadmap after `F18`.

`F19` exists to turn the preserved `F9` family from placeholder storage into a
decision-complete near-term roadmap. It does not authorize runtime execution
and it does not claim arbitrary `C` or unrestricted Wasm support.

This roadmap preserves:

- `H38` as the active docs-only decision packet;
- `H36` as the underlying active routing/refreeze packet;
- `F18` as the long-arc route-fixing bundle;
- `F9` as the preferred forward family;
- `R42`, `R43`, and `R44` as deferred future gates only;
- `no active downstream runtime lane`.
