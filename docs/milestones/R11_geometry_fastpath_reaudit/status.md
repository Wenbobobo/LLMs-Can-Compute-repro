# Status

Provisioned on 2026-03-20 under active `H14`.

- near-term target: exactness, fast-path behavior, and negative controls on the
  current geometry stack;
- reference code lives in `src/geometry/hardmax.py` and
  `src/geometry/hull_kv.py`;
- current historical references are `R4` mechanistic closure and `R10`
  same-endpoint cost attribution.
- current execution interface is now captured in `runbook.md` and
  `artifact_contract.md`;
- current reopened artifact lands in `results/R11_geometry_fastpath_reaudit/`.
