# Brainstorm Log

- Treat `step_budget` as a provenance target, not just a runtime bucket.
- Compare first semantic divergence against later looping behavior.
- Keep the same staged checkpoint so provenance and `M4-D` remain comparable.
