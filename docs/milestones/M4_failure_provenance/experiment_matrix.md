# Experiment Matrix

- Inputs:
  `M4-D` staged failure digest,
  reference traces,
  produced traces where available.
- Outputs:
  provenance classes,
  per-program first-bad-decision rows,
  updated `C2h` wording only if the attribution changes the claim boundary.
