# Blocked Hypotheses

- A fourth compatibility regime should be proposed before provenance is mapped.
- Capacity or data rescue should be reopened while `C2h` is already a negative
  closure.
- `step_budget` should be treated as a terminal explanation rather than a
  symptom class.
