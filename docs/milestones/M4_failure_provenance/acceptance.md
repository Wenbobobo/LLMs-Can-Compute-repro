# Acceptance

- Every non-exact held-out `M4-D` row receives one provenance class.
- `step_budget` failures are split into genuine recurrent loops vs earlier
  semantic mistakes that caused non-halting rollout.
- The milestone updates `C2h` wording only if the new provenance changes the
  interpretation materially.
