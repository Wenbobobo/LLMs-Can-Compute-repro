# Stop Rules

- stop if the proposal cannot name a materially different cost structure
- stop if the useful-case target is broader than the preserved narrow endpoint
- stop if transparent linear or external baselines are missing
- stop if the claimed route is only another same-lane microvariant
- stop if runtime is requested before a later explicit authorization packet
