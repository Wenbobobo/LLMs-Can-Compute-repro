# Todo

- [x] Decide whether `R17` exposes a bounded repair target.
- [x] Define the smallest useful counterfactual or repair probe around
  `helper_checkpoint_braid_long` retrieval.
- [x] Decide that the first repair probe is one narrower decomposition-only
  counterfactual (`partitioned_exact` memory retrieval).
- [x] Keep outputs comparator-only rather than claim-bearing by default.
- [x] Run the focused target/control probe and export trace-address profiles.
- [x] Try the bounded pointer-like exact follow-up on the same target plus
  matched control.
- [x] Decide whether a staged deterministic retrieval probe is still needed
  after the pointer-like follow-up.
- [x] Close `R18` and hand the packet to `H17` after the bounded pointer-like
  probe materially changes the same-surface runtime picture.
