# R18 D0 Same-Endpoint Runtime Repair Counterfactual

Comparator-only same-endpoint repair lane under `H16`. `R18a` first tested one
decomp-first exact memory counterfactual on the bounded
`helper_checkpoint_braid_long/retrieval_total` target named by `R17`, against
matched control `stack_memory_braid`. `R18b` then closed the packet with a
pointer-like exact retrieval runtime probe on the same target/control pair and
an exact `8/8` admitted-surface confirmation sweep.
