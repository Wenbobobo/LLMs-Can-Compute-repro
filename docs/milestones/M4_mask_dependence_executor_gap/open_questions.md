# Open Questions

These questions are non-blocking for the current plan.

- If `opcode_shape` failures disperse across many fields, should the milestone
  close immediately as a caveated bridge result?
- If a fourth regime is justified, is the likely target field-compatibility,
  operand-family legality, or control-flow skeleton legality?
