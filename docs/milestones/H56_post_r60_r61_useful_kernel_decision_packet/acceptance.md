# Acceptance

`H56` is acceptable only if it:

- stays docs-only;
- records one explicit selected outcome;
- preserves the no-widening defaults; and
- leaves any later scope lift to a separate explicit packet.
