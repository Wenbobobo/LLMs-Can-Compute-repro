# P32 Todo

- refresh `export_h0_release_hygiene.py` so regenerated `H0` wording stops
  calling `D0` the whole current paper endpoint and instead treats it as the
  preserved first compiled step inside the broader current `H43` endpoint;
- refresh `export_p3_paper_freeze.py` so unsupported arbitrary-`C` wording
  treats `D0` as preserved first compiled support rather than the whole
  current endpoint;
- regenerate the affected `H0/P3` machine-readable outputs;
- add a focused `P32` exporter plus tests/results for the historical wording
  refresh packet; and
- record the completed auxiliary packet in status, plan, milestone,
  publication-record, and active-wave handoff surfaces without displacing
  `P31`.
