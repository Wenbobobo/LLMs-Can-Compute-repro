# P32 Post-H43 Historical Wording Refresh

Completed auxiliary historical/regeneration wording refresh packet downstream
of landed `H43` and current low-priority `P31`.

`P32` exists to refresh the remaining preserved machine-readable and
regeneration-facing surfaces that still describe `D0` as the current compiled
endpoint, especially the regenerated `H0` release-hygiene audit and `P3`
paper-freeze unsupported-claim wording. It is an operational/docs sidecar,
not a scientific gate, and it does not displace `P31` as the current
low-priority wave, authorize a new runtime lane, or merge `main`.
