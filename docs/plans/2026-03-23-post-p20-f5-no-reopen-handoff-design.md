# 2026-03-23 Post-P20/F5 No-Reopen Handoff Design

## Summary

`P20_post_h34_manuscript_narrative_resync` is now complete.

`F5_post_h34_contradiction_scout_matrix` is now complete and ends at:

- `no_reopen_candidate_survives`

The current paper-facing and planning-facing state is therefore coherent:

- `H32_post_r38_compiled_boundary_refreeze` remains the active routing packet;
- `H34_post_r39_later_explicit_scope_decision_packet` remains the current
  docs-only control packet;
- `R39_origin_compiler_control_surface_dependency_audit` remains completed
  downstream evidence, not a routing change;
- there is still no active downstream runtime lane.

## Why Save This Handoff

The repo now has three different kinds of post-`H34` material:

1. landed current-control packets (`H32/H33/R39/H34`);
2. paper-facing resync work (`P20`);
3. planning-only reopen filtration (`F5`).

Without one short handoff design, later unattended work can still drift into
two bad patterns:

- treating `P20` as if it reopened the science lane just because the
  manuscript now reads cleanly;
- treating `F5` as if it implied "keep searching until a reopen candidate
  appears" instead of "freeze unless one unique contradiction is named in
  advance."

## Selected State

Selected state:

- preserve the current no-reopen interpretation;
- treat `P20` and `F5` as completed closeout/support lanes;
- restrict near-term work to docs-only maintenance, planning-only future
  gating, or later explicit contradiction design if new evidence appears.

Not selected:

- create `H35` by default;
- authorize another same-substrate runtime batch by momentum;
- widen to broader compiler, Wasm-like, arbitrary-`C`, or frontier/demo scope.

## Admissible Near-Term Work

The only justified near-term work after `P20/F5` is:

1. keep repository entrypoints, milestone indexes, and historical handoff docs
   synchronized to the now-complete `P20/F5` outcome;
2. continue planning-only frontier or future-stage gating downstream of `H34`
   without authorizing execution;
3. if a genuinely sharper contradiction appears later, save a new explicit
   design before any execution and keep it on the same substrate unless a
   separate scope decision says otherwise;
4. continue routine commit/worktree hygiene without merging back into dirty
   `main` by momentum.

## Future Reopen Rule

Any future same-substrate reopen after this handoff must begin with a new
design packet that names:

- one unique contradiction or sharper dependency;
- one predeclared comparator set;
- one fixed success/failure criterion;
- one explicit stop condition;
- no hidden scope lift.

Until then, the correct next-order statement remains:

- no active downstream runtime lane

## Save Rule

Save this handoff before later unattended planning starts from the post-`H34`
state.

- if later planning is substantial, use a fresh clean worktree;
- if later work splits into independent documentation or analysis bundles, use
  subagents on disjoint write scopes;
- do not let planning surfaces override landed packet state.
