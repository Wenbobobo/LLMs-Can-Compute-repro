# H7 Refreeze and Record Sync Design

## Goal

Refreeze the repo after `H6/R3/R4/(optional R5)` by synchronizing ledgers,
standing audits, and milestone digests without reopening broader scope.

## Intended outputs

- one `H7` milestone scaffold;
- refreshed `README.md`, `STATUS.md`, publication ledgers, and experiment
  manifest rows;
- refreshed standing-audit results and milestone result digests;
- green targeted tests, `pytest`, and `git diff --check`.

## Acceptance

- the repo returns to one narrow active surface after the current packet;
- all standing audits are green on the new driver wording;
- active claims remain bounded to the same `D0` endpoint.
