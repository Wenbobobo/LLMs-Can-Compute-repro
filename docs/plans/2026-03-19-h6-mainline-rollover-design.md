# H6 Mainline Rollover and Backlog Sync Design

## Goal

Turn the completed `H4/E1a/E1b/H5` packet into a finished baseline and switch
the repo to a new bounded active packet centered on `D0` exact-execution
stress and mechanistic closure.

## Intended outputs

- one saved master plan in `tmp/`;
- one updated `current_stage_driver.md` naming `H6`, `R3`, `R4`, optional
  `R5`, conditional `E1c`, and `H7`;
- synchronized `README.md`, `STATUS.md`, and publication-record entry docs;
- one `H6` milestone scaffold;
- one machine-readable `H6` rollover guard;
- one backlog cleanup pass that removes stale active-looking checkboxes from
  historical milestones.

## Acceptance

- the repo no longer presents `H4/E1a/E1b/H5` as the live active packet;
- the new active packet is explicitly `H6/R3/R4/(optional R5)/H7`;
- no doc weakens `M7` no-widening or broadens the claim surface;
- historical milestones stop presenting dormant items as active backlog.
