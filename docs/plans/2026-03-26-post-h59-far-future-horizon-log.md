# 2026-03-26 Post-H59 Far-Future Horizon Log

## Purpose

This note stores route ideas that are scientifically interesting but not ready
to enter the current execution screen. It exists to avoid forgetting them while
keeping the current branch conservative.

## Stored Routes

### High-cost model route

Trainable or transformed model routes may still matter later if they offer a
genuinely different efficiency regime on a useful exact target. That does not
make them current candidates. They remain `high_cost_model_route_far_future_only`.

### Programs-into-weights route

The stronger idea of compiling logic directly into weights may matter only on a
much later horizon where the question changes from narrow repo reproduction to a
different research program. It remains
`programs_into_weights_route_far_future_only`.

## Exclusion Rule

Neither stored route is execution-ready now. They do not enter the current
`F34` reopen screen, they do not reopen `F27/R53/R54`, and they do not weaken
the current `H58/F32/H59` stop boundary.

## Later Revisit Conditions

A later revisit requires:

1. a new explicit packet;
2. a materially different cost model or scientific target;
3. a useful-case exact baseline contract; and
4. a clear reason why the route is better than stopping or archiving.
