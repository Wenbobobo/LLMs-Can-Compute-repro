# R9 D0 Real-Trace Precision Boundary Design

## Goal

Keep the precision story companion-only on current or newly admitted `D0`
rows and sharpen the real-trace boundary without reopening an open-ended sweep.

## Intended outputs

- one unified boundary summary;
- one per-family boundary table;
- one negative-control companion;
- one failure-taxonomy note.

## Scope lock

- only current or newly admitted same-endpoint rows;
- fixed scheme set and bounded base/horizon grid;
- no new robustness claim beyond the current suite.

## Acceptance

- the suite can be summarized as “effective here / unproven here / negated
  here” without further open-ended sweeps;
- the boundary remains explicitly companion-only.
