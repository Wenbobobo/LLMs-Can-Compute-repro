# Post-H15 Same-Scope Reopen Design

## Goal

Reopen the repository after `H15_refreeze_and_decision_sync` without changing
the scientific scope. The next packet should strengthen evidence on the same
fixed `D0` endpoint rather than widening by narrative momentum.

## Stage chain

- `H16_post_h15_same_scope_reopen_and_scope_lock`
- `R15_d0_remaining_family_retrieval_pressure_gate`
- `R16_d0_real_trace_precision_boundary_saturation`
- `R17_d0_full_surface_runtime_bridge`
- optional `R18_d0_same_endpoint_runtime_repair_counterfactual`
- `H17_refreeze_and_conditional_frontier_recheck`

## Why this chain

`R12` already made the harder-slice inventory explicit across `24` staged
`R6` rows and `4` staged `R8` rows. The next data-backed move is therefore to
extend the same retrieval-pressure accounting to the four `R6` families that
were left outside `R8`, then saturate the precision companion only on the
admitted same-scope rows, and only then revisit runtime profiling on the
expanded admitted surface.

## Acceptance discipline

- keep the endpoint fixed to tiny typed-bytecode `D0`;
- keep geometry/runtime wording bounded to what current artifacts support;
- preserve positive and negative results symmetrically;
- route only true contradictions to `E1c`;
- allow `R18` only as a same-endpoint comparator/repair lane;
- require `H17` before any future frontier recheck.
