# 2026-03-26 Post-H59 Archive And Reopen Screen Design

## Purpose

This wave converts the landed post-`H59` planning-only state into a cleaner
archive/publication/hygiene/next-lane packet stack:

- preserve `H59` as the explicit reproduction-gap packet;
- keep `H58` and `F32` scientifically binding;
- screen exactly one conditionally credible future reopen family without
  authorizing runtime work;
- lock outward wording to the honest post-`R62` state;
- record repo/worktree hygiene and merge posture explicitly; and
- store farther-future model/weights ideas without promoting them into current
  execution candidates.

This is still a docs-and-control wave. It does not reopen runtime execution.

## Current Scientific Reading

The current stack still supports a narrow positive and a broader negative at
the same time:

1. append-only retrieval-backed execution is real on the admitted narrow
   surfaces;
2. `H43` remains a real paper-grade endpoint for the useful-case exact lane;
   and
3. the strongest current executor-value lane is closed by `R62/H58/F32/H59`.

The repo therefore should not drift into one more same-lane executor-value
microvariant. The correct next docs-level action is to preserve the closeout,
name the only conditionally credible later reopen family, and make stop/archive
the default if that family never becomes execution-ready.

## Output Packets

This wave lands five packet families.

1. `P43_post_h59_repo_graph_hygiene_and_merge_map`
   records the preferred repo-local worktree graph, alias posture, and explicit
   no-merge rule for dirty root `main`.
2. `P44_post_h59_publication_surface_and_claim_lock`
   aligns outward wording to the post-`H59` archive state and keeps the claim
   boundary honest.
3. `F34_post_h59_compiled_online_retrieval_reopen_screen`
   becomes the current planning bundle and screens only one conditionally
   credible future reopen family.
4. `H60_post_f34_next_lane_decision_packet`
   becomes the new active docs-only packet and selects planning-only /
   archive / stop as the live posture.
5. `F35_post_h59_far_future_model_and_weights_horizon_log`
   stores farther-future route ideas without authorizing them.

## Conditional Reopen Rule

The only currently admissible later reopen family is:

- `compiled_online_exact_retrieval_primitive_or_attention_coprocessor_route`

That family is still conditional. Before any runtime work starts later, it must
first satisfy all of the following:

1. change the cost structure materially rather than renaming the current lane;
2. preserve an exact useful-case or clearly stronger narrow comparator target;
3. state the baseline contract and stop rules before execution;
4. run from a clean successor worktree under `D:/zWenbo/AI/wt/...`; and
5. receive later explicit authorization rather than inheriting momentum from
   `R62`.

If these conditions are not satisfied, the correct outcome remains
planning-only, archive, or explicit stop.

## Excluded Near-Term Routes

The following are not current execution candidates:

- same-lane executor-value microvariants on the existing runtime path;
- weakly renamed `R62` follow-ups;
- the high-cost-but-possibly-efficient model route; and
- programs-into-weights / compile-logic-into-weights routes.

The first class is inadmissible because it does not change the cost structure.
The latter two remain far-future storage only and are recorded in `F35`, not in
the active screen.

## Execution Notes

- Save the plan before execution.
- Keep dirty root `main` quarantined.
- Prefer `uv` for exporter and test runs.
- Keep raw rows and tracked artifacts above roughly `10 MiB` out of git by
  default.
- Treat new GPTPro input as optional future advisory input, not as a reason to
  reopen the current lane automatically.
